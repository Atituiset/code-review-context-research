# 概念篇：从 AST 到 PDG——代码分析技术的深度阶梯

> 面向刚接触代码分析领域的工程师。不需要编译原理背景，每一段都从"它能回答什么问题"出发。
> 读完本篇，你应该能看懂任何静态分析工具的技术选型，并判断它处于分析深度的哪一层。
> 姊妹篇：《02-实践篇-五工具深度对比与检视工具链》，把这里的概念对应到五个真实项目。

---

## 0. 一个总问题

所有代码分析工具，本质上都在回答同一个问题的不同精度版本：

> **"这段代码是什么意思，它会怎么执行？"**

回答这个问题有一条深度阶梯，每往下一层，答案更接近运行时真相，但构建成本也大约高一个数量级：

```
文本(行) → CST(语法形状) → AST(语义符号) → CFG(过程内控制流)
        → ICFG/CG(跨函数控制流) → PDG(数据/控制依赖) → Value-Flow(值流)
   便宜、鲁棒、浅                                    昂贵、脆弱、深
```

本篇逐层讲清楚每一级是什么、怎么构建、能回答什么、不能回答什么。

---

## 1. 第一层：文本与正则

最原始的分析方式：把代码当字符串。ripgrep、grep、正则。

- **能回答**："这个字符串/模式出现在哪些文件的哪些行？"
- **不能回答**：任何关于"意义"的问题。`foo` 是函数名还是变量名还是注释里的单词，文本层不知道。
- **工程价值**：速度极快（百万行秒级）、零依赖、对任何脏代码鲁棒。在所有分层架构里它都是第一道粗筛——误报允许，靠上层过滤。

这一层没有"树"，跳过。

---

## 2. 第二层：CST（具体语法树）——tree-sitter 的世界

### 2.1 什么是 CST

对源码做**词法 + 语法分析**（parsing），得到一棵树。关键性质：**每一个 token 都在树上**——括号、分号、逗号、注释，一个不少。

```c
a = (b + c) * 2;
```

CST 上能逐字节重建源文件（"无损"）。括号虽然不改变运算含义，但作为 token 挂在树上。

### 2.2 tree-sitter 是什么

tree-sitter 是一个**解析器生成器 + 增量解析运行时**：

- 每种语言的语法用声明式的 `grammar.js` 描述，生成一个 **GLR** 解析器；
- **错误容忍**：代码写了一半、有语法错误，照样 parse，出错位置标为局部 `ERROR` 节点，其余部分不受影响；
- **增量解析**：改几行代码只重 parse 受影响的子树，毫秒级——这是它能支撑编辑器实时高亮的原因；
- **无语义分析**：这是最重要的边界，下面详述。

### 2.3 tree-sitter 的天花板：它不做消歧

C/C++ 是上下文相关语法，很多写法**光看语法无法确定含义**：

```c
T * x;        // 声明 T* 指针？还是 T 乘以 x？
foo<A, B>(c); // 模板调用？还是 (foo<A) > (B>c) 的比较运算？
```

要分辨，必须知道 `T` 是不是一个已声明的类型——这是**符号表**信息，属于语义。tree-sitter 不维护符号表，它靠 GLR 把歧义的两条路都试出来，再用 grammar 里写的 dynamic precedence（动态优先级）选一棵"语法上最像"的。**它可能选错**，因为决定对错的信息它根本没有。

同理，在 tree-sitter 的树里：

```c
foo(x);
```

`foo` 只是一个 `identifier` 节点，内容是字符串 `"foo"`。这个函数定义在哪个文件、有几个同名重载、是否被宏改写——树上完全没有答案。

### 2.4 这一层能回答什么

- **能**：结构模式匹配（"找出所有 if 里调用 memset 的地方"）、语法层面的导航和索引、对脏代码鲁棒的全仓扫描。
- **不能**：符号身份（"这个 foo 是哪个 foo"）、类型、宏展开后的真实代码、真实调用目标。

ast-grep、tree-sitter query 是这一层的模式匹配查询接口，本质是"语法形状匹配"，不是语义查询。

---

## 3. 第三层：AST（抽象语法树）——Clang 的世界

### 3.1 Clang 的完整流水线

Clang AST 不是 parse 完就有的，它是编译器前端**全流水线**的产物：

```
源码 → 预处理(宏展开/条件编译求值) → 词法 → 语法 → 语义分析 → AST
```

与 tree-sitter 相比多了两端：前面的**预处理**，和后面的**语义分析**。这两个恰恰是信息含量最高的阶段。

### 3.2 "抽象"是什么意思

同一段 `a = (b + c) * 2;`，Clang AST 只保留语义骨架：

```
BinaryOperator '*'
├── BinaryOperator '+'
│   ├── DeclRefExpr 'b' ──→ VarDecl(b)
│   └── DeclRefExpr 'c' ──→ VarDecl(c)
└── IntegerLiteral 2
```

- 括号没了（优先级已编码进树结构）、分号没了；
- 但**多出了你代码里没写的东西**：`ImplicitCastExpr`（隐式类型转换）、临时对象的构造/析构等隐式节点；
- 最关键的是 `DeclRefExpr` 后面那个箭头：每个标识符的引用都**直接指向它的声明节点**，并带 **USR**（Unified Symbol Resolution，跨文件、跨翻译单元唯一的符号 ID）。

同名函数、重载、命名空间屏蔽——在语义分析阶段全部解析完毕。消歧靠的是真正的符号表，不是猜。

### 3.3 宏：两棵树看到的完全是两个世界

```c
#ifdef CONFIG_SPDU
DECLARE_STRUCT(Foo)
#endif
```

- **Clang AST**：`#ifdef` 已按当前编译配置求值，`DECLARE_STRUCT(Foo)` 已展开成真实的 `struct Foo { ... }` 声明。它看到的是**某个具体编译配置下**的代码。
- **tree-sitter CST**：看到的是未展开的原始文本——宏调用本身、`#ifdef` 两个分支都在树上，但它**不知道哪个分支生效**。

对多单板、多 defconfig 宏控的 C/C++ 代码库，这个差异是致命的：tree-sitter 方案从根本上不知道自己分析的是哪个产品形态的代码。

### 3.4 代价

- **必须能编译**：需要 `compile_commands.json`、头文件齐全、宏定义正确，一处错误污染整个翻译单元；
- **贵**：大翻译单元秒级解析、GB 级内存；全仓索引（clangd-indexer）在万级 TU 的代码库上是小时级、几十 GB 内存的工程问题；
- **不可增量**：任何改动整个 TU 重 parse（clangd 靠 preamble 等技巧缓解）。

### 3.5 对比总表

| | Clang AST | tree-sitter CST |
|---|---|---|
| 流水线终点 | 语义分析后 | 语法分析后 |
| token 保留 | 否（抽象） | 是（无损，含注释） |
| 隐式节点 | 有（隐式转换等） | 无 |
| 标识符 | 指向声明的引用（带 USR） | 字符串 |
| 消歧 | 符号表（正确） | GLR + 优先级（可能错） |
| 宏 | 真实展开 | 原样保留，不知分支 |
| 前置条件 | 必须可编译 | 任何文本 |
| 错误容忍 | 差 | 强（局部 ERROR 节点） |
| 增量 | 无（TU 级） | 有（毫秒级） |
| 速度/内存 | 慢/大 | 快/小 |

一句话：**Clang AST 是"编译器理解代码后的内部表示"，tree-sitter CST 是"代码语法形状的无损快照"**。前者回答"是什么"，后者回答"长什么样"。

---

## 4. 第四层：CFG（控制流图）——从"结构"到"执行"

AST 回答"代码是什么"，CFG 开始回答"**代码会怎么执行**"。

### 4.1 定义

把单个函数体内的语句归并成**基本块**（basic block：一段顺序执行、中间无跳转的语句序列），块之间的边表示执行转移：

```c
if (p == NULL)        // Block 1: 条件判断
    return -1;        // Block 2
x = *p;               // Block 3
```

```
Block 1 ──真──→ Block 2 ──→ exit
   └──假──→ Block 3 ────────→ exit
```

if/else 分叉、循环回边、switch 跳转、异常路径，全部显式化。Clang 在 `clang/lib/Analysis/CFG.cpp` 中从 AST 构建 CFG，它是 clang static analyzer 的底座。

### 4.2 关键性质

- **过程内（intraprocedural）**：一个函数一张图，函数边界是墙；
- CFG 级分析能回答："这个解引用是否在某个分支上先于空指针检查执行"——AST 级只能看到"它们都在这个函数里"。

---

## 5. 第五层：Call Graph 与 ICFG——跨越函数边界

### 5.1 Call Graph

节点是函数，边是"可能调用"。构建它需要把每个调用点解析到目标函数——虚函数、函数指针让它变成指向分析（points-to analysis）问题，精度和成本都是 trade-off。

### 5.2 ICFG（过程间控制流图）

比 call graph 更强的结构：把各个函数的 CFG 用两类边缝起来——

- **调用边（call edge）**：调用点所在 block → 被调函数的 entry block；
- **返回边（return edge）**：被调函数的 exit block → 调用点的**后继** block。

为什么要区分调用边和返回边？为了**路径可行性**。一条穿越多个函数的执行路径，必须满足 call/return 配对：从 f 的调用点 A 进入 g，就必须从 g 返回到 A 的后继，而不是 f 的另一个调用点 B 的后继。不满足配对的路径在现实中不可能发生，ICFG 可以把它们排除——这直接决定缺陷报告的误报率。

### 5.3 共同盲区：表驱动分发

无论 call graph 还是 ICFG，遇到这类代码都会断：

```c
const MsgHandler g_dispatch[] = {
    { MSG_1001, sess_handle_invite },   // 这是数据，不是调用
};
// 某处：tbl[i].handler(msg);           // 调用目标静态不可知
```

`MSG_1001 → sess_handle_invite` 这条"控制流"编码在**数据**里，任何通用图分析都看不见。电信/嵌入式代码大量使用这种表驱动架构，这是通用工具链的实际缺口（实践中需要领域知识显式提取，见实践篇 navmap）。

---

## 6. 第六层：PDG（程序依赖图）——"谁影响谁"

PDG 在 CFG 之上叠加两类依赖边：

- **数据依赖**：语句 A 用到的值由语句 B 定义（def-use 链）；
- **控制依赖**：语句 A 是否执行取决于谓词 B 的真假。

```c
x = input();      // (1)
if (x > 0)        // (2)
    strcpy(d, x); // (3) 数据依赖于(1)，控制依赖于(2)
```

PDG 是两种核心分析技术的图论基础：

- **程序切片（slicing）**："影响语句 (3) 的所有语句"= PDG 上反向可达集合；
- **污点分析（taint）**："用户输入能否流到 strcpy" = 源到汇在 PDG 上的可达性。

再往上还有 value-flow graph（如 SVF 在 LLVM IR 上构建的稀疏值流图），追踪值穿越函数边界的流动，精度更高、成本也更高——全程序构建在大型代码库上是小时级。

### 6.1 深度与成本的账

| 层 | 能确认的结论 | 典型成本（百万行 C/C++） |
|---|---|---|
| AST 级 | "这是可疑模式" | 秒~分钟 |
| CFG 级 | "危险操作在某分支可达" | 分钟 |
| ICFG 级 | "存在一条跨函数的可行执行路径" | 分钟~小时 |
| PDG 级 | "污点真的能从源流到汇" | 小时级全程序分析 |

每往下一层，误报率大约低一个数量级——这就是为什么告警工具（AST 级）和定罪工具（路径/污点级）必须分层使用。

---

## 7. 另一个流派：关系化 + Datalog

上面讲的是"图"的路线，还有一个并行流派：**把代码关系数据化，用声明式语言查询**——代表是 CodeQL（QL 语言）和蚂蚁 CodeFuse-Query（Gödel 语言），底层都是 Datalog。

思路：把 AST/调用关系/继承关系落成**关系型数据表**（事实），分析规则写成声明式**规则**，引擎做不动点推导，自动得出所有满足条件的结果。

为什么是 Datalog 而不是 SQL：Datalog 天然支持递归规则且保证**单调性和终止性**。"调用链上所有可达函数"这种传递闭包，SQL 写起来很痛苦，Datalog 是一条规则；且不会因规则写法不当而不收敛。

代价与现状：数据要先离线建库（抽取器成本高），且关系化的深度目前以 AST/调用图级为主流，CFG/PDG 级的关系化即使在工业界也仍在建设中——因为依赖分析需要接近编译器的精度，成本压不住。

---

## 8. 总结：一张图记住全部

```
文本 ──→ CST ──→ AST ──→ CFG ──→ ICFG/CG ──→ PDG ──→ Value-Flow
rg      tree-   clang   clang   自定义构建    依赖分析   SVF 等
        sitter  AST     CFG.cpp
│       │       │       │       │           │          │
找模式  找结构  认符号  分支    跨函数路径   污点可达   值流追踪
便宜鲁棒                        │           │          昂贵精确
                                └──── 表驱动分发是全段盲区 ────┘
```

选型时只问三个问题：

1. **我要确认的结论属于哪一层？**（模式？符号？路径？污点？）
2. **我的代码能不能编译？**（能 → clang 路线可达深层；不能 → tree-sitter 是上限）
3. **我的代码的控制流是不是藏在数据里？**（表驱动架构 → 需要领域知识显式化）

三个问题答完，工具链的形状自然浮现。实践篇用五个真实项目演示这个过程。

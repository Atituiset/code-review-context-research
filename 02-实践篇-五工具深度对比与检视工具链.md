# 实践篇：五个工具的深度对比与 AI 代码检视工具链

> 面向资深工程师。基于对五个本地仓库的源码、提交历史与架构文档的实际勘察（2026-08）。
> 概念基础见姊妹篇《01-概念篇-从AST到PDG》，本篇不再重复原理，直接讲选型、分工与集成。
>
> 勘察对象（均为 GitHub 仓库）：
> - [Atituiset/mcp-language-server](https://github.com/Atituiset/mcp-language-server)（fork，上游 [isaacphi/mcp-language-server](https://github.com/isaacphi/mcp-language-server)）
> - [Atituiset/codegraph](https://github.com/Atituiset/codegraph)（fork，上游 [colbymchenry/codegraph](https://github.com/colbymchenry/codegraph)）
> - [Atituiset/navmap](https://github.com/Atituiset/navmap)（自研）
> - vul-llvm（本地 fork，未推送远端；上游 [thebesttv/vul-llvm](https://github.com/thebesttv/vul-llvm)，基于 llvmorg-17.0.6）
> - [Atituiset/CodeFuse-Query](https://github.com/Atituiset/CodeFuse-Query)（fork，蚂蚁开源 [codefuse-ai/CodeFuse-Query](https://github.com/codefuse-ai/CodeFuse-Query)）

---

## 1. 一句话结论

五个项目恰好各占分析深度阶梯的一层，是**互补而非竞争**关系：

```
codegraph ──→ mcp-language-server ──→ navmap ──→ vul-llvm
"找得到"      "认得准"                "业务怎么走"  "路径是否可行"
CST/启发式图   clangd 语义符号          领域语义显式化   CFG/ICFG 可行路径
便宜、全仓     精确、按需               补全图盲区       定罪级证据

CodeFuse-Query：关系化+Datalog 流派的参照系，对当前 C/C++ 场景价值有限（beta）
```

> **生产决策更新（§6）**：agent 检视场景已弃用 mcp-language-server（clangd 索引
> 静默不完整的失败模式不可接受），由 codegraph + navmap 组合替代；
> §1-§4 的分层分析仍成立，§4.1 流水线中的"语义过滤"层以逃生通道形式保留。

---

## 2. 逐项目勘察记录

### 2.1 mcp-language-server（你的 fork）：三层漏斗 + CodeAtom IR

**技术底座**：Go 1.24，MCP server 包装 LSP（生产用 clangd），stdio/NDJSON JSON-RPC。
上游是 isaacphi/mcp-language-server（原版仅 definition/references/diagnostics/hover/rename 等薄封装），
你的 fork 已重写为面向**只读代码检视**的架构（`docs/architecture-and-guide.md`）：

```
L1 文本层  ripgrep            → 行级命中，无语义
L2 语法层  tree-sitter/ast-grep → CST 节点，局部语法
L3 符号层  clangd (LSP)        → USR 级精确语义
         ↓
CodeAtom IR 归一化 → 物理吞并 → 语义去重 → 预算裁剪 → 喂给 LLM
```

关键设计（`docs/code-atom-ir.md`）：

- **CodeAtom IR**：`SemanticID / Name / Kind / FilePath / [StartByte, EndByte] / L0-L2 载荷 / SourceTool / Priority`。
  空间坐标用字节偏移（区间计算的唯一真理，不用行号）；语义身份 USR 不可得时降级为 `name@path`。
- **三维去重**：扫描线物理吞并（`MergePhysical`）+ 语义去重（`DedupSemantic`，保留最高 Priority）
  + 四相预算裁剪（`CropBudget`，L0→L1→L2→丢弃，计费含渲染开销）。
- **实测**（u-boot，176 万行，`search auto TODO`）：5569 条原始命中 → 1683 原子 → 8KB 预算输出 7,892B。
- **工具面**：默认 7 个只读工具（search/definition/references/callers/callees/diagnostics/hover）；
  编辑工具 `MCP_LS_ENABLE_EDITS` 门控、8 个 debug 工具 `MCP_LS_DEBUG_TOOLS` 门控。
- **生产化痕迹**：daemon+proxy 模式（多 agent 进程共享一份 clangd，避免 N×clangd OOM）；
  clangd 并发 RWMutex+信号量改造；clangd 18 vs 21 并发压测；14000+ TU 内存分析；
  跨文件 LSP preload 设计。这些说明它已经在真实大仓上趟过坑，不是原型。

**定位**：语义符号层（L3）是护城河——`callers/callees` 走 clangd callHierarchy，编译级精度，
tree-sitter 方案给不了。L1/L2 是便宜的前置过滤。

### 2.2 codegraph（你的 fork）：纯 tree-sitter 路线的预建索引

**技术底座**：TypeScript 编排 + Rust 内核（`codegraph-kernel/`，tree-sitter 0.25 原生解析，
每语言一个 walker；TS 侧 wasm 降级路径）。全仓预建索引存 **SQLite**（better-sqlite3），
查询零解析、毫秒级。对外暴露 CLI + MCP server。

**关键架构事实**：

- 调用边是 **resolution 阶段的启发式推导**（receiver 类型推断、同名方法消歧），
  不是真正的语义解析——会漏会错，这是路线的固有限制，不是 bug。
- fork 的 `main` 上**没有偏离上游的私有提交**。C/C++ 硬化主要以"上游贡献 + fork 跟进"
  形式存在，其中疑似团队作者的提交：
  - `8e697a0`（xyyxr）：UE 反射宏类恢复（in-body reflection macros）；
  - `85e9ac6` + `11acc50`（ctype_lab）：C/C++/ObjC/Rust union 声明索引 + kernel 奇偶测试。
- 上游自身的 C/C++ 硬化同样被继承：宏污染函数名通用恢复（`cb20a3b`）、
  C 函数指针调用边（`cfnptr.rs`，pass 230→151s）、命名空间/模板限定名组合
  （`e437918`/`4dd29ea`）、CUDA/Metal 支持、R7a C/C++ walker 内核化（`2d72891`）。
  **这个上游对 C/C++ 的投入强度，在 tree-sitter 系工具里是罕见的。**

**定位**： eager（预建索引）vs mcp-language-server 的 lazy（按需查询）。
冷启动后查询极快、零编译依赖；但不知道宏哪个分支生效、调用关系是猜的。

### 2.3 navmap（自研）：领域语义的显式化

**技术底座**：Python + libclang（与生产 clangd 严格同版 20.1.0，启动强制校验，
`navmap/clangenv.py`；vendor 二进制来自 conda-forge `libclang13-20.1.0`）。

**提取对象**（电信/嵌入式表驱动架构的四类导航图）：

| 提取器 | 产出 | 覆盖形态 |
|---|---|---|
| dispatch | 静态分发表 `msg_id → handler` | 普通宏表/乱序指定初始化/X-Macro/`#ifdef` 表项/cast 与 `&` 形式 handler |
| registry | 注册式分发（虚拟表 `registry:<ApiName>`） | `MsgReg(MSG, fn)` 调用点，API 名单配置驱动 |
| statemachine | `{state, event, handler, next_state}` 四元组 | 表驱动状态机；switch 手写状态机明确不做 |
| globalvar | 全局变量读写清单 + 布局 | 引用分类 assign/compound/incdec/addr(保守记写)/读 |

**漏斗流水线**（与 mcp-language-server 同一哲学）：

```
全仓 → 正则粗筛（误报允许，漏报不允许）→ compdb 当参数字典（不当扫描队列，
避开 clangd-indexer 全 TU 成本）→ 只解析候选文件 → JSON + markdown 双产物
```

增量 refresh 走 `git diff` 圈定受影响集合（5 条规则，全不命中则零重解析）。

**实测**（2026-08-17）：u-boot 176 万行 → 1228 候选 → 19 表 131 表项，USR/位置零缺失，
全量分钟级；srsRAN（现代 C++）则需要配 `register_apis` 走 registry 提取器。

**为什么它不可替代**：表驱动分发是**所有**通用图工具的盲区——tree-sitter 看到 InitListExpr，
clangd callHierarchy 在 `tbl[i].handler(msg)` 处断掉，vul-llvm 的 ICFG 同样在函数指针查表处断边。
navmap 用配置化领域知识（注册 API 名单、状态机字段映射）把"数据里的隐式控制流"显式化。
这绕开了函数指针指向分析在大代码库上精度/成本双输的死局。

### 2.4 vul-llvm：编译器级 CFG/ICFG 的缺陷路径工具

**技术底座**：llvm-project 17.0.6 fork（源自 thebesttv/vul-llvm），改动克制但位置深：

- 修改 `clang/lib/Analysis/CFG.cpp`：保证函数调用落在 CFGBlock 的最后一条语句——
  让"调用点 block"和"后继 block"有干净划分，ICFG 的 call/return 边才好接。
- 新增 `clang/tools/thebesttv/`（含 `ICFG.cpp`）：三种边——`INTRA_PROC`（过程内）、
  `CALL_EDGE`（调用点 block → 被调 entry）、`RETURN_EDGE`（被调 exit → 调用点后继），
  保证 call/return 配对，生成的路径是**可行路径**。
- `graph-generation/`：缺陷路径生成工具 + 测试用例族（NPE/double-free/resource-leak/
  宏/模板/同名函数等 corner case）。

**定位**：产出"从源到汇的一条可行执行路径"——这是把"可疑"变成"实锤"的关键一跃，
也是给 LLM 最好的 prompt 材料（一条具体路径胜过一百条告警）。
代价是必须随 LLVM 源码树构建，是重武器路线。

### 2.5 CodeFuse-Query：关系化 + Datalog 流派

**技术底座**：蚂蚁开源。COREF 数据模型（= AST + ASG + CFG + PDG + CallGraph +
ClassHierarchy + Documentation）落成关系型数据表，Gödel 查询语言（基于 Datalog，
声明式、保证单调性与终止性），Sparrow CLI 本地化。

**现实状态**（README 自述）：Java/JS/TS/Go 成熟；**OC/C++ beta**（397 节点但信息不全）；
**CFG 和 PDG 全语言"仍在建设中"**——即使 Java 也只有部分 CFG。
这反映行业现状：AST 级关系化（CodeQL 的核心）已成熟，CFG/PDG 级关系化在工业界 still hard。

**对本场景的判断**：你们是多单板宏控 C/C++，clang 直出路线（vul-llvm/navmap）比
抽取器路线靠谱；CodeFuse-Query 当前主要价值是**架构参照**（Datalog 表达递归查询的优雅），
以及 Java 系团队的场景。

---

## 3. 横向对比

| 维度 | codegraph | mcp-language-server | navmap | vul-llvm | CodeFuse-Query |
|---|---|---|---|---|---|
| 分析层 | CST+启发式图 | 语义符号(clangd) | 语义AST+领域知识 | CFG/ICFG | 关系化(AST为主) |
| 数据形态 | 预建SQLite(eager) | 按需查询(lazy) | 离线产物JSON/md | 按需构建 | 离线建库 |
| 编译依赖 | 无 | compile_commands.json | compile_commands.json | 随LLVM树构建 | 抽取器 |
| 调用关系 | 启发式，会漏错 | callHierarchy，精确 | 表/注册分发显式映射 | 静态精确，函数指针处断 | 语言beta |
| 宏 | 不知分支 | 真实展开 | 真实展开+条件栈记录 | 真实展开 | — |
| 表驱动分发 | 盲区 | 盲区 | **核心能力** | 断边点 | 盲区 |
| 冷启动 | 建索引一次后极快 | clangd索引慢(万级TU) | 分钟级 | 重 | 重 |

---

## 4. 工具链分工与集成机会

### 4.1 推荐的分层流水线（AI 漏洞检视场景）

```
[1] 宽进    codegraph / ripgrep      模式候选，便宜、覆盖全
[2] 语义过滤 mcp-language-server      确认符号身份，去同名误报，补精确引用
[3] 业务导航 navmap                   msg→handler、状态机迁移、全局变量读写
[4] 路径定罪 vul-llvm                 可行执行路径，实锤证据
            ↓
     CodeAtom IR 归一化 → 预算裁剪 → LLM 检视 prompt
```

### 4.2 两个具体的高价值集成点

**集成点一：navmap 补 vul-llvm 的 ICFG 断边（最值钱）**

vul-llvm 生成缺陷路径遇到 `tbl[i].handler(msg)` 就断；navmap 的 dispatch/registry
产物恰好持有 `msg_id → handler USR` 完整映射。把这张表作为**虚拟调用边**注入 ICFG
（查表调用点 → 所有可能 handler 的 entry），缺陷路径即可穿透分发层。
反向收益：navmap 每条 handler 的 USR 与 clangd/mcp-language-server 天然对齐
（navmap README 已注明 MVP 期用 USR 对齐）。

**集成点二：navmap/vul-llvm 产物接入 CodeAtom IR**

- navmap 表项作为一类原子（如 `Kind=DISPATCH_ENTRY`，Priority 拉高）：
  AI 检视 handler 时最缺的上游上下文——"谁触发我、我在哪个状态机迁移里"——直接入 prompt。
- vul-llvm 缺陷路径作为最高优先级原子：LLM 拿到完整罪证链而非散点线索。
- navmap 的 markdown 产物本来就是为 prompt 注入设计的，接入成本极低。

### 4.3 当前缺口

1. **证据模型未统一**：navmap 与 vul-llvm 仍是独立产物，未汇入 CodeAtom IR；
2. **ICFG 断边未补**：函数指针/查表分发处的路径断裂尚无解（集成点一就是方案）；
3. **navmap 与 clangd 索引的一致性校验**未实现（navmap 设计文档 §7 自列）；
4. **PDG/污点层整体空缺**：目前最深到 ICFG 可行路径，"污点是否真能从源到汇"
   还依赖路径级人工/LLM 判断，没有自动化数据依赖分析兜底。

---

## 5. 给团队引入 codegraph 的决策建议

基于勘察事实，codegraph 在你们体系中的合理位置是**替代/增强 L1+L2**（预建索引 +
图遍历比裸 ripgrep/tree-sitter 更强），而 L3 的 clangd 终审不可替代：

- **适合**：无编译依赖环境的快速导航、全仓符号/引用粗查、agent 高频低精度查询；
- **不适合**：作为检视结论的唯一依据——宏分支不知情 + 调用边启发式 +
  表驱动分发盲区，三个限制在你们宏控 C/C++ 场景下都会实际命中；
- **落地建议**：挑一个有函数指针 + 宏控的真实模块做对照实验——codegraph 调用边 vs
  clangd callHierarchy vs navmap 显式映射，量化三方差异后再定引入深度。
  这比读任何文档（包括本文）都更能决定引入策略。

---

## 6. 生产决策（2026-08）：agent 检视场景弃用 clangd 路线，codegraph + navmap 替代

### 6.1 决策背景

生产检视系统此前用 mcp-language-server，团队在类 Claude Code 的 agent 检视场景中
决定不再使用它。表面原因是"重"（clangd 建索引耗时），但根本原因是**失败模式**：

> clangd 路线的索引完整度不可验证，而索引不完整直接导致信息提取不完整——
> 对检视场景，漏报没有信号。"references 返回 3 条"和"索引只建了 40% 所以只找到
> 3 条"无法区分。静默不完整比没有工具更危险：它制造虚假的确定感。

此前为 mcp-language-server 做的 daemon+proxy、preload、14000+ TU 内存分析，
本质都在对抗同一个根源：**全量语义索引在大仓上贵且脆**。

### 6.2 为什么 codegraph + navmap 能解

两者的核心性质是**完整性与可编译性解耦，且失败显式可见**：

- codegraph：tree-sitter 逐文件独立解析，不依赖 compile_commands.json 质量；
  解析失败逐文件可见，索引覆盖率 = 文件数，可验证。
- navmap：粗筛保证不漏（误报允许）；compdb 只当参数字典，借参失败的候选
  记录在产物里，覆盖率可监控。

失败模式从"静默漏"变为"显式列"——这是替代成立的第一理由。

### 6.3 能力映射与净损益

| mcp-language-server 工具 | 替代 | 变化 |
|---|---|---|
| `search`（auto） | codegraph explore + agent 原生 Grep | L1/L2 持平，L3 语义降级为启发式 |
| `definition` | codegraph 符号索引 | 大体持平 |
| `references` | codegraph refs | 启发式，可能多/漏 |
| `callers`/`callees` | codegraph 调用边 + navmap 分发/注册表 | 通用调用降级；**分发链路反而从无到有** |
| `diagnostics` | 无替代，移给 CI | 丢失 |
| `hover` | codegraph 签名/docstring 部分覆盖 | 降级 |

`callers/callees` 的账要细算：在表驱动电信代码里，clangd callHierarchy **本来就在
查表处断**——handler 的真实触发路径从不在其中。替代后通用调用降级为启发式，
但分发链路的 callers 信息从无到有（navmap）。对该代码形态，实际损耗远小于账面。

### 6.4 目标架构

```
Claude Code 类 Agent
├── 原生 Read/Grep/Glob          → L1 文本
├── codegraph MCP                 → 符号导航/引用/启发式调用边/图遍历
├── navmap 产物（nightly CI）     → msg→handler、状态机、全局变量读写
│    （MCP tool 或 markdown 注入两选一）
└── 逃生通道：mcp-language-server 不默认加载，
    保留在"终审/争议确认"环境按需启用
```

### 6.5 落地顺序

1. **navmap 进 CI**（零侵入，先见效）：nightly 增量 refresh，产物直接 prompt 注入；
   监控 compdb 单配置覆盖面（多单板场景合并多份 compdb 或接受显式失败记录）。
2. **codegraph 对照实验后定深度**：§5 的三方对照实验先行；同步完成两项核查——
   遥测行为（`TELEMETRY.md`，企业内必须确认关闭方式）、fork 与上游的同步策略
   （团队 C/C++ 修复继续走上游 PR，避免分叉）。
3. **navmap → codegraph 虚拟调用边注入**（本架构下从加分项变为核心项）：
   `msg_id → handler USR` 写入 codegraph 图库，遍历即可跨分发边界。
   没有 clangd 终审后，分发链路的连通性全靠它。

### 6.6 需要新建的薄层

- 虚拟边注入脚本（navmap JSON → codegraph 图库新边类型）；
- 调用边可信度标注：启发式边在输出中打标，prompt 模板约定"线索非证据"；
- navmap 产物的 MCP 封装（或直接文件注入）。

### 6.7 遗留风险

- **精确语义终审缺位**：启发式调用边的静默错误无自动兜底，靠 §6.4 逃生通道
  和流程纪律（结论回源码确认）兜住；
- **索引新鲜度负担转移**：codegraph 增量 sync（`src/sync/`）的 reindex 延迟
  成为新的关键路径，需验证是否满足检视时效；
- **路径级定罪仍空缺**：codegraph+navmap 只到"谁可能调用谁"，可行路径判断
  （vul-llvm 层）不在本架构内，检视 prompt 需明确此边界。

---

## 附：勘察依据

- mcp-language-server：`docs/architecture-and-guide.md`、`docs/code-atom-ir.md`、
  `docs/MCP_TOOLS_ZH.md`、git log（ast-grep 集成 `f812a8b`、daemon+proxy `f5004c7`、
  并发改造 `215d353` 等）
- codegraph：`codegraph-kernel/Cargo.toml`、`src/extraction/`、`src/db/schema.sql`、
  git log 作者与提交分析（上游 PR #1101-#1576 的 C/C++ 硬化线）
- navmap：`README.md`（版本纪律/漏斗/实测）、`docs/navmap-libclang-extractor-design.md`
- vul-llvm：`readme.org`（缺陷路径生成工具说明）、`graph-generation/` 用例族
- CodeFuse-Query：`README_cn.md`（COREF 定义与语言成熟度自评）

# 落地建议篇：生产落地路线与验收标准

> 综合《01 概念》《02 自有工具链》《03 行业调研》《04 工程实践》四篇的最终建议。
> 前提已锁定：agent 检视场景弃用 mcp-language-server（clangd 全量索引），
> 以 **codegraph + navmap** 为核心重建。本文只讲怎么落地、怎么验收、怎么防坑。
> 日期：2026-08。

---

## 1. 目标架构（终态）

```
┌─────────────────────────────────────────────────────────────┐
│                    Claude Code 类 Agent                       │
│                                                               │
│  [L1] 原生 Grep/Glob/Read        文本层（免费获得）            │
│  [L2] codegraph（MCP+CLI）       结构导航/引用/启发式调用边     │
│  [L2.5] navmap 产物（nightly）   领域导航：分发表/状态机/全局量 │
│         └─ 以 hook 模式注入（不靠 agent 自觉查询）              │
│  [Intent] 事故复盘/设计决策 RAG   "为什么"层（相似度≥阈值才注入）│
│                                                               │
│  评审回路：scout（圈疑点）→ 深度评审（核实）                     │
│           → verifier（符号存在性校验 + disprove-it 证伪）       │
│                                                               │
│  逃生通道：mcp-language-server（终审环境按需，不默认加载）       │
└─────────────────────────────────────────────────────────────┘
        底座纪律：完整性可验证 · token 预算显式 · 失败显式可见
```

设计依据：Snap 的 Zoekt+agent loop（04 §1.3）证明"精确检索底座 + 智能在循环里"
是大规模正解；GitHub Copilot 的教训（04 §8.1）证明工具指令必须按评审任务写；
DoorDash（04 §2）证明发现/验证分离和精确率优先是信任的前提。

---

## 2. 分阶段路线

### Phase 0：对照实验与评估基线（约 2 周）——先量清楚，再定深度

**这是所有后续工作的地基，跳过这一步后面都是拍脑袋。**

1. **三方对照实验**（02 篇 §5 已提，现在扩展为四方）：
   挑 2~3 个有代表性的真实模块（必须包含：函数指针查表 + 宏控多分支 + 同名符号密集），
   在同一组评审问题上对比：
   - codegraph 预计算调用边；
   - **agentic 实时查找**（ripgrep 组合，Snap 路线——04 §1.3 的分歧点必须实测）；
   - clangd callHierarchy（真值基准，用逃生通道跑）；
   - navmap 显式映射（分发链路部分）。
   产出：各路线的 precision/recall/成本矩阵，决定 codegraph 边的信任等级标注策略。
2. **建评估集 v1**（DashBench 方法，04 §2.4）：
   - 从真实评审漏报和线上事故回溯 20~30 例（知道"应该抓到什么"）；
   - **必须包含良性改动样本**（测克制力，否则假阳性永远测不出）；
   - 包含后被 revert/hotfix 的 PR（测能不能拦住真实回归）。
3. **工程核查**：codegraph 遥测行为确认与关闭（`TELEMETRY.md`）；
   fork 与上游同步策略成文（团队 C/C++ 修复继续走上游 PR）。

**Phase 0 出口标准**：信任等级策略成文（哪些查询结果可直入 prompt、
哪些必须标注"启发式"、哪些必须回源码确认）；评估集可用。

### Phase 1：双工具上线（约 1 个月）

1. **navmap 进 CI**（风险最低，先做）：nightly 增量 refresh，产物 JSON+md 落仓；
   compdb 解析失败率纳入监控（单 defconfig 覆盖面是已知边界）；
   名单扩充沿用 suggest-apis/vars 人审，加 DoorDash 三重过滤
   （CI 能抓的、模型已知的、无证据的——不进配置）。
2. **codegraph 部署**：每仓建索引 + 增量 sync；MCP 与 CLI 双暴露
   （紧循环用 CLI，省协议开销——03 §9.3）；
   **验证索引新鲜度**：push 后到可查询的延迟必须满足检视时效
   （这是 clangd 时代负担的转移点，02 §6.7）。
3. **工具 description 按评审任务改写**（GitHub 教训，04 §8.1）：
   不用通用编码助手措辞；明确"锚定改动→收窄→最小证据"的调用范式；
   每个工具标注输出可信度等级。
4. **navmap 产物改 hook 注入**（GitNexus 模式，03 §9.5）：
   agent 读 handler 文件时自动附带其分发/状态机上下文，
   不依赖 agent 记得去查。

**Phase 1 出口标准**：agent 检视会话中 L1+L2+L2.5 可用；索引覆盖率、
新鲜度、navmap 失败率三个指标有 dashboard。

### Phase 2：评审回路硬化（约 1~2 个月）——信任是在这一层建立的

1. **scout/verify 分离**（DoorDash v3，04 §2.1）：
   scout 只圈疑点不验证（产出调查线索清单），深度评审只对线索核实。
   关键收益是**过滤掉 95% 不值得深看的改动**。
2. **verifier 闸门**，两道检查（都便宜，都该抄）：
   - **符号存在性校验**（Snap，04 §1.2）：结论引用的每个符号必须能在
     供给上下文里找到，找不到直接撤稿——这是启发式调用边静默错误的
     零基建缓解（02 §6.7 遗留风险的正面回答）；
   - **disprove-it 证伪 pass**（DoorDash，04 §2.3）：发出前显式尝试
     推翻自己的发现。宁可少发。
3. **navmap → codegraph 虚拟调用边注入**（02 §6.5 第三步）：
   `msg_id → handler USR` 写入 codegraph 图库，遍历跨分发边界。
   本架构下分发链路的连通性全靠它。
4. **反馈飞轮**：每条结论可 👍/👎，信号入 ground truth 数据集；
   **被忽略和被修复的发现分别统计**（Snap Finding Lifecycle，04 §1.2）。

**Phase 2 出口标准**：评估集上加权精确率 ≥85%（DoorDash 生产水平 87%）；
良性样本零误报；连续两周生产采纳率可测量。

### Phase 3：Intent 层与基准运营化（约 1 个月）

1. **事故复盘/设计决策 RAG**（国内实践，04 §4）：
   历史复盘文档 embedding 入库；评审时以"改动意图摘要"为查询，
   **相似度 ≥0.85 才注入**（低于阈值的历史案例会带幻觉，宁可不召回）。
   这是"当前代码与某次历史事故逻辑模式相似"的预警能力，
   该团队上线后老事故复发率明显下降。
2. **评估基准运营化**：评估集持续吃进新案例（新漏报、新事故），
   良性样本同步更新；每次 prompt/工具/模型变更先过评估集再上线
   （DashBench 的 feedback loop 定位）。
3. **成本治理**：按 **cost per successful review** 计量而非 token 单价
   （弱模型重试更贵的教训，04 §2.4）；scout 用便宜模型、verify 用强模型的
   分级配置在评估集上定。

**Phase 3 出口标准**：Intent 层上线且注入率/命中率可观测；
基准回归纳入发版流程。

### 后续选项（不排期，视 Phase 0~3 结果决定）

- **Hybrid LSP 调研**（codebase-memory-mcp，MIT，03 §5.1）：
  若对照实验显示启发式调用边误差不满足终审场景，这是不回到 clangd
  进程模型的升级路径。优先级高于 stack graphs（上游已降温，04 §8.3）。
- **PoC 沙箱验证**（DeepAudit/codebadger 路线，03 §6、04 §8.2）：
  漏洞类结论加"能复现才算实锤"的一道闸。
- **vul-llvm 集成**：可行路径证据接入，作为高危发现的定罪层
  （02 §4.2 集成点一）。成本重，仅在检视结论需要路径级证据的场景启用。
- **SCIP 化逃生通道**：若终审需求多人化，clangd 索引产物离线共享，
  而非每人一份活 clangd（03 §5.3）。

---

## 3. 验收指标（写进项目目标）

| 指标 | 目标 | 行业参照 |
|---|---|---|
| 加权精确率（评估集） | ≥85% | DoorDash 87% |
| 良性样本误报 | 0 | Snap golden dataset 0% |
| 生产采纳率（高危发现） | ≥55%，逐季提升 | DoorDash 60.2% |
| 单次评审成本 | 团队自定上限，按 successful review 计 | Snap $0.40 / DoorDash $3 |
| 评审时延 | < 人类首次评审（分钟级） | Snap 10min vs 人类 5h |
| 索引覆盖率可见性 | 100% 可查询、失败显式列出 | 弃用 clangd 的第一理由 |
| 索引新鲜度 | push 后分钟级 | Snap 秒~分钟 |

推广纪律（Snap 经验，04 §1.4）：**不默认强推**。先 opt-in，用
"这个漏掉的 bug 工具本可抓到"的案例驱动团队主动要求默认开启——
被强推的评审工具会被静音，被静音的工具抓不到任何东西。

---

## 4. 风险登记册

| 风险 | 缓解 | 状态 |
|---|---|---|
| 启发式调用边静默错误 | 可信度标注 + verifier 符号校验 + 逃生通道 | Phase 0 定级 / Phase 2 上闸 |
| 索引新鲜度不达标 | Phase 1 实测延迟；不达标则事件触发 reindex | 待验证 |
| 表驱动分发残余盲区 | navmap 名单持续人审 + 虚拟边注入 | Phase 2 |
| compdb 单配置覆盖不全 | 失败率监控；多 compdb 合并 | Phase 1 |
| 评审噪音杀死信任 | 三重过滤 + 良性基准 + disprove-it | Phase 2 |
| RAG 召回幻觉（Intent 层） | 相似度 ≥0.85 硬阈值 + 误报反馈微调 | Phase 3 |
| fork 与上游分叉 | C/C++ 修复一律走上游 PR | Phase 0 成文 |
| 工具指令误导 agent 行为 | 评审专用 description + trace 级回归 | Phase 1 |

---

## 5. 十条军规（从四篇调研蒸馏，可打印）

1. 上下文质量 > 模型能力；漏报先查上下文，别先换模型。
2. 完整性必须可验证；静默不完整的工具比没有更危险。
3. 发现与验证分离；单 pass 直出评论必然幻觉。
4. 精确率优先；被静音的评审工具价值为零。
5. 每条结论过符号存在性校验 + 自我证伪，再发出。
6. token 预算显式建模；先排序后裁剪。
7. 精确检索是底座，语义层只做加法不做地基。
8. CI 能抓的、模型已知的、无证据的，一律不进 prompt。
9. 评估集必须有良性样本；只看分的评估是自欺，要看 trace。
10. 采纳靠价值自发；用"本可抓到"的案例推广，不靠行政命令。

---

## 附：本文结论的出处索引

- 架构与决策：02 篇 §6（生产决策）、§4.2（集成点）
- 对照实验设计：02 篇 §5 + 04 篇 §1.3（Snap 分歧点）、§7
- 评审回路：04 篇 §1.2（Snap Verifier）、§2.1/2.3（DoorDash scout/disprove-it）
- Intent 层：04 篇 §3（Unblocked）、§4（国内 RAG 实践）
- 评估方法：04 篇 §2.4（DashBench）、§6 共识清单
- 工具设计纪律：04 篇 §8.1（GitHub Copilot 工具指令教训）
- 升级路径：03 篇 §5.1（Hybrid LSP）、§5.2（stack graphs，已降档）、§6（CPG/PoC）

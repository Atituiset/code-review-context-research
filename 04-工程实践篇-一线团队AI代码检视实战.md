# 工程实践篇：一线团队的 AI 代码检视实战经验

> 调研时间 2026-08。本篇只收录**一手实践分享**：企业工程博客的生产系统复盘、
> 个人工程师博客、社区经验帖。与《03-行业调研篇》（工具与学术）互补——
> 03 回答"有什么"，本篇回答"真正跑在生产里的系统长什么样、踩过什么坑"。
> 所有内容附来源；个别原文被反爬的，用可靠二手综述替代并注明。

---

## 0. 一页结论

五个生产系统（Snap、DoorDash、Unblocked、国内某业务团队、以及 Anthropic 自己的
Claude Code）在完全不同的约束下独立收敛到了同一组结论：

1. **上下文质量 > 模型能力**——漏报几乎总是上下文问题，不是模型问题（Snap 原话）；
2. **"发现"与"验证"必须分离**——单 pass 直出评论必然幻觉，所有成熟系统都有
   scout/verify 两阶段 + 一道 verifier 闸门；
3. **精确率优先于召回率**——噪音比漏报更快杀死工具（DoorDash、Unblocked 同结论）；
4. **精确检索是底座，语义层是可选插件**——Snap 明确"赌 RAG 输"，embedding 只做加法；
5. **凡是需要"能编译"才能索引的方案，在大规模异构仓上直接出局**——
   Snap 原话点名 Kythe，与我们弃用 clangd 索引的决策完全同频；
6. **评估闭环是生产与玩具的分水岭**——DashBench、golden dataset、
   thumbs up/down 反馈飞轮，家家都建了。

---

## 1. Snap：CodePal + Code Search（两篇，最完整的技术披露）

来源：[Part 1: CodePal](https://eng.snap.com/codepal)、
[Part 2: Code Search](https://eng.snap.com/code_search)。
规模：数千仓、近 TB 源码；CodePal 4 个月 20 万次评审、覆盖 90% PR、
单次评审 ~$0.40、10 分钟内完成（人类首次评审中位 ~5 小时）。

### 1.1 CodePal 的上下文构建（与我们的漏斗高度同构）

- **两遍文件选择器**：第一遍用 **tree-sitter** 解析全仓建"符号→文件"索引；
  第二遍分析 diff 提取被引用符号，查索引、按符号重叠度给文件打分，
  **在 token 预算内选 top N**。——这就是 codegraph 建索引 + CodeAtom 预算裁剪
  的 Snap 版本，验证了我们架构的 mainstream 性。
- **No-clone 架构**：评审全程在内存中跑，只打 GitHub Enterprise API，
  不落盘、不镜像仓库。"clone-per-review 会把基础设施或 GHE 压垮"。
  一次评审只取几百 KB 源码，与仓库大小无关。
- **一份上下文喂三个产品**：父 workflow 只做一次符号索引和文件选择，
  评审/摘要/PR 描述三个子任务共享——"摘要和描述约等于免费"。

### 1.2 Review Loop：多 pass + 一致性闸门 + Verifier

- 两个 bootstrap pass 并行（同模型不同采样参数），**比较结果判断模型"真的相信"
  哪些发现**；两 pass 一致则丢弃投机性的第三 pass（agreement gate），
  不一致则第三 pass 的工作计入；之后"发现新东西才触发下一个 pass"，
  diff 枯竭即收敛。
- **Verifier 是独立长对话**，逐条审计发现——包括检查**发现中引用的每个符号
  是否真的出现在供给模型的上下文里**，引用模型没见过的函数直接撤稿。
  这个"符号存在性校验"是对付幻觉的低成本高收益手段，可直接抄。

### 1.3 Code Search：agent-first 的检索基础设施

- **Zoekt**（trigram 索引）分片部署，全组织索引，push 后秒~分钟级新鲜，
  p95 亚秒。关键决策：**语言无关、直接从 git tree 索引文本，零 per-language 接入**。
  原话："*Anything that needs a working build to index (like Kythe) is a
  non-starter at this scale.*"——需要能编译才能索引的方案在大规模异构仓上
  直接出局。这与我们弃用 clangd 全量索引的理由一字不差。
- **AI search 不是 RAG，是 agent loop**：自然语言问题 → 模型规划 5~10 次
  精确搜索 → 评估 → 合成带引用的答案。"检索系统是笨的，智能在循环里。"
- **明确赌 RAG 输**：向量库是有状态的运维负担（GPU 成本、与源码一致性、
  安全），而精确搜索无状态；"模型在变强，智能在循环里的系统**免费继承
  模型进步**，RAG 的天花板是检索器"。embedding 未来只做加法（无关键词锚点的
  真语义查询），不做地基。
- **`find_callers` 不是预计算调用图**：它是"用调好的 prompt 让 search agent
  回答 who-calls 问题"，底层仍然只有精确搜索 + agent loop 两个机制。
  这是与我们 codegraph（预计算启发式调用边）**相反的工业实践**，
  值得在对照实验中专门对比（见 §7）。

### 1.4 运营数据

召回率（真阳性）30% → 80% 的提升来自四项：**更深的代码理解（跨文件符号解析 +
  扩大语言解析）、多 pass review loop、检测面 8→12 类、增量评审**。
golden dataset 假阳性降到 0%（held-out）。采纳不靠强推：9% opt-in 起步，
工程师在漏掉 CodePal 本可抓到的 bug 后**主动要求默认开启**。
核心教训原文："*Context over top tier model*——报给我们的漏报，更大的问题
几乎总是供给模型的上下文，而不是模型不够强。"

## 2. DoorDash：scout/verify 三段式 + DashBench 评估基准

来源：DoorDash 工程博客两篇（[评审 agent](https://careersatdoordash.com/blog/doordash-built-an-ai-code-reviewer-engineers-actually-listen-to/)、
[信任与评估](https://careersatdoordash.com/blog/how-we-learned-to-trust-our-ai-code-reviewer-at-doordash/)，
原文有反爬，细节引自 [ZenML LLMOps 案例综述一](https://www.zenml.io/llmops-database/building-a-production-ai-code-review-agent-with-high-engineer-acceptance)
与[综述二](https://www.zenml.io/llmops-database/building-trust-in-ai-code-review-through-comprehensive-benchmarking)）。
规模：每周 1 万+ PR、56 仓；高危发现采纳率 60.2%（第三方工具此前 46%）；
~$3/次、7 分钟。

### 2.1 三版架构演进（每版的教训都无法提前预判）

- **v1 专科 agent 扇出**（安全/测试/性能各管一摊）：机械 bug 抓得住，
  **架构性问题全漏**——没有任何一个专科能看到大图。
- **v2 两个通用评审各看全量上下文**：能看到大图了，但单个会话责任过重，
  注意力稀释，关键问题淹没。
- **v3 lead scout + 两个深度评审**：scout 只负责"觉得可疑"（不验证），
  产出调查线索清单；深度评审只对少数线索做彻底核实。
  **隐含的另一半收益是过滤掉了 95% 不值得深看的改动**。
  官方自评：这就是资深工程师审代码的方式——先凭直觉圈点，再深挖。

### 2.2 领域评审档案（review profiles）——对 AGENTS.md 的批判

- 明确**反对**"把 AGENTS.md/CLAUDE.md 全量塞进 prompt"：那是为写代码优化的，
  噪音太大。改为从四个来源**挖掘只与评审相关的知识**：AGENTS.md 里的不变量、
  历史 PR 评审中资深工程师反复标记的模式、Slack 里的设计决策与"别做 X"讨论、
  事故复盘。
- **每条候选规则必须过三重过滤**：CI 能抓的丢弃、模型训练数据里有的丢弃、
  无具体 file-and-line 证据的丢弃——剩下的才是真正组织特有的隐性知识。
- **按改动路由档案**：改支付网关的 PR 加载 PSP/金额安全规则，
  改推荐流的 PR 拿到完全不同的规则集。"审欺诈改动的 agent 和审定价改动的
  agent 实际上是不同的评审者。"——这是 56 个异构仓采纳率一致的原因。

### 2.3 精确率优先 + "disprove-it" 闸门

- 评论发出前必须过一道**自我证伪 pass**：系统显式尝试推翻自己的发现，
  经不起推敲的整条丢弃。宁可少发，不刷 PR。
- "正确的发现也可能是烂评论"：宽泛的总结、无力的"建议检查一下"、
  重复评审时的重复评论，都在侵蚀信任。**找不到行动点的发现宁可不发**。

### 2.4 DashBench：不信任任何单一信号

- 洞察："工程师采纳"只填充混淆矩阵两格（TP/FP），漏报（FN）和正确的沉默（TN）
  完全不可见；且**人类采纳 ≠ 发现真实**（作者因赶进度、改法侵入性等
   workflow 因素拒绝真实发现）。
- 数据集从 ~1000 候选 PR 精选 105 例，含"良性 PR"（测克制力）和
  "后被 revert/hotfix 的 PR"（测能不能拦住真实回归）；
  标注用原作者标注 + LLM judge + 人工仲裁三方三角验证。
- 结果（105 例）：scout+reviewer 两段式比单 pass 基线**加权召回 53.6% vs 30.7%**
  （164→504 条真实发现），加权精确率 87%；成本 $3.91 vs $0.75。
  不同模型组合在不同维度各有胜负，**没有配置全面占优**——
  "更好"必须指明在哪个轴上。
- 成本教训："最便宜的模型不等于最便宜的评审"——弱模型输出非法 JSON 反复
  重试，强模型一次过。**计量单位是 cost per successful review，不是 token 单价。**

## 3. Unblocked：组织记忆与"评论的社会成本"

来源：[Unblocked 工程博客](https://getunblocked.com/blog/building-ai-code-review-with-context-as-a-first-class-system/)。

- 定位：上下文的瓶颈不在代码内，而在**组织知识**——Slack 讨论、历史 PR、
  RFC、事故复盘。经典案例：PR 给 API client 加重试逻辑，diff 看着没问题，
  但四个月前平台团队在 Slack 里刚解释过为什么**删掉**这里的重试（掩盖级联故障）。
  "代码孤立地看是对的，在上下文里是错的。"
- **验证管线丢弃首轮 50%+ 的发现**，宁可漏掉次要风格问题也不打扰工程师。
- 两个反直觉的用户行为发现：
  1. 开发者**讨厌"显而易见正确"的评论**（指出 lint 失败、改动导致测试挂了
     这类 CI 已知的）——"技术上正确但没有信息量"；
  2. 开发者会**删掉喜欢的 bot 评论**——只为在资深同事来看之前保持 PR 干净。
     "每条评论都有社会成本。"
- LLM 继承了人类评审者的偏见：**satisfaction of search**（找到一个 bug 后
  警惕性下降）和 **null finding anxiety**（找不到问题就生造冷门问题）。
  解法来自可靠性工程：结构化 checklist pass、对抗性验证、prompt 里显式
  声明偏见。

## 4. 国内实践：RAG 召回"历史事故"做业务级评审

来源：[掘金：如何用 AI 做业务级 Code Review](https://juejin.cn/post/7611165150921621554)。

- 痛点定义很本土："类似的 bug 出现过，复盘文档也有，然而新人不知道，
  老人记不清。"目标：做一个**有记忆、懂业务、看过线上事故**的评审。
- 链路：GitLab Webhook → diff 预处理（剔除 lock/样式/静态资源，按文件+行数
  切片，**先让模型回答"这段改动在做什么"再进入评审**）→ 把改动意图 embedding
  后去向量库检索**历史事故复盘/最佳实践**，相似度 ≥0.85 的 top2 注入 prompt →
  深度评审。"当前代码与历史某次事故的逻辑模式相似度达 85%，请重点关注"——
  上线后**老事故复发率明显下降**。
- 这正是 CodeRabbit 上下文三分法里 **Intent（意图）层**的一种实现：
  代码结构上下文之外的"为什么"上下文。
- 也坦承了 RAG 的坑：召回"似是而非"的历史案例会带幻觉，
  靠相似度阈值过滤 + 误报反馈微调检索策略。

## 5. Anthropic 自己的实证：RAG 曾经输给 agentic search

Claude Code 作者 Boris Cherny 在 Hacker News 上确认（转引自
[KaraxAI 的系统级拆解](https://karaxai.com/posts/how-claude-code-works-systems-deep-dive/)
与 [vadim.blog](https://vadim.blog/claude-code-no-indexing/)）：

> "Early versions used RAG + a local vector db, but we found pretty quickly
> that agentic search generally works better."

即：**最有利于 embedding RAG 的团队（自家就有向量产品）实测后放弃了它**。
Snap 的"赌 RAG 输"不是孤例，是有公开实测记录支撑的决策。

（反方观点也存在：[Milvus 的批评](https://milvus.io/blog/why-im-against-claude-codes-grep-only-retrieval-it-just-burns-too-many-tokens.md)
认为 grep-only 烧 token——其利益相关方是向量数据库厂商，且与 Snap 的实测
"agentic 搜索 token 更省"相左；原文有反爬未逐句核实，仅标注其立场。）

## 6. 跨实践的共识清单（可直接当 checklist 用）

| # | 共识 | 证据方 |
|---|---|---|
| 1 | 上下文质量 > 模型能力 | Snap（"context over top tier model"）、Unblocked |
| 2 | 发现/验证分离 + verifier 闸门 | DoorDash scout/reviewer、Snap 多pass+Verifier、Unblocked 丢弃50% |
| 3 | 精确率优先，噪音杀死工具 | DoorDash 60.2%采纳率哲学、Unblocked 社会成本论 |
| 4 | 符号存在性校验防幻觉 | Snap Verifier（引用符号必须在上下文中） |
| 5 | 精确检索是底座，语义是插件 | Snap（Zoekt+agent loop）、Claude Code（弃 RAG）、GrepRAG |
| 6 | 需编译才能索引的方案出局 | Snap 点名 Kythe；与 clangd 索引痛点同源 |
| 7 | token 预算显式建模 | Snap 两遍选择器 top N、国内 30k token 分片 |
| 8 | CI 能抓的、模型已知的，不进 prompt | DoorDash 三重过滤、Unblocked"显而易见正确"评论被反感 |
| 9 | 评估闭环（基准+反馈飞轮）是分水岭 | DashBench、Snap golden dataset、 thumbs up/down |
| 10 | 组织/意图上下文独立于代码上下文 | Unblocked 组织记忆、国内事故复盘 RAG、DoorDash review profiles |
| 11 | 成本按 successful review 计，非 token 单价 | DoorDash（弱模型重试更贵）；Snap $0.40 vs DoorDash $3 |
| 12 | 采纳靠价值自发，不靠强推 | Snap 9%→90% 靠"漏掉的 bug CodePal 本可抓到" |

## 7. 映射回我们的 codegraph + navmap 架构

**被验证的决策**：

- tree-sitter 符号索引 + token 预算选择上下文 ≈ Snap 两遍选择器（§1.1）——
  我们的 codegraph + CodeAtom 裁剪与生产级实践同构；
- "不需要编译即可索引"作为硬约束 ≈ Snap 对 Kythe 的判断（§1.3）——
  弃用 clangd 全量索引的决策有了第二个大厂背书；
- navmap 的领域知识显式化 ≈ DoorDash review profiles 的"组织特有隐性知识"（§2.2）——
  都是"模型训练数据里没有、CI 抓不到、有具体证据"的知识层。

**需要正视的分歧**：

- **Snap 不做预计算调用图**，`find_callers` 是 agent loop 实时组合的（§1.3）；
  我们的 codegraph 是预计算启发式调用边。两种路线的取舍：预计算=快且便宜但会
  静默错，实时组合=新鲜可解释但每次付费。**建议在对照实验中加上这一项**：
  codegraph 调用边 vs agentic 实时查找（ripgrep 组合）vs clangd 真值，
  在我们的表驱动 C/C++ 上量一次。
- 我们的工具链目前只有"代码结构上下文"，**Intent 层（历史事故、设计决策、
  复盘文档）空白**——Unblocked（§3）和国内实践（§4）都证明这一层对
  "业务级评审"是刚需，且实现成本不高（文档 embedding + 相似度阈值）。

**可直接抄的三个机制**：

1. **符号存在性校验**（Snap §1.2）：检视结论引用的符号必须能在供给上下文里
   找到，找不到就撤稿——对我们"启发式调用边静默错误"是零基建的缓解；
2. **三重过滤的规则准入**（DoorDash §2.2）：CI 能抓的、模型已知的、无证据的
   规则不进 prompt——适用于 navmap 名单扩充（suggest-apis/vars 的人审环节
   可加这两条过滤）；
3. **良性 PR 进基准集**（DashBench §2.4）：评估集里必须放"干净改动"测克制力，
   否则假阳性率永远测不出来。

---

## 8. GitHub：官方工程实践 + 开源生态扫描

> 本节数据用 `gh` CLI 直连 GitHub API 采集（2026-08-25 star 数），
> 官方实践引自 GitHub 工程博客原文。

### 8.1 GitHub 官方：两个不同层面的实践

**Blackbird 代码搜索引擎**（[A brief history of code search at GitHub](https://github.blog/engineering/architecture-optimization/a-brief-history-of-code-search-at-github/)）：

- Elasticsearch 索引 800 万仓要数月，且通用文本引擎在代码域有根本性错配——
  **标点是有意义的 token**（`!=`、`->`、`::`）、**词干提取必须关闭**
  （搜 `fmt` 不是 `format`）、必须支持正则。GitHub 最终用 Rust 从零写
  Blackbird：增量索引、分片、45M 仓 / 115TB。
- 与 Snap 选 Zoekt 是同一个判断：**代码搜索需要专用索引，通用方案到此为止**。
  这也是 GrepRAG 结论在基础设施侧的镜像。

**Copilot code review 的工具指令教训**（[Better tools made Copilot code review worse](https://github.blog/ai-and-ml/github-copilot/better-tools-made-copilot-code-review-worse-heres-how-we-actually-improved-it/)，2026-07，强烈推荐原文）：

- 把评审 agent 的自研探索工具换成 Copilot CLI 共享的 `grep`/`glob`/`view`——
  工具客观上更好了，基准却**变差了**：成本上升、抓到的问题变少。
- 根因不在工具在**指令**：共享工具的说明是为"写代码的助手"写的（先把仓摸一遍），
  评审需要的是"从 diff 出发、问定向问题、最小证据确认"。agent 陷入了
  "宽泛搜索→猜路径→泛读→积累上下文"的 browsing loop。
- 把工具指令改写成评审者工作流（锚定 diff → grep/glob 收窄 → 批量便宜发现 →
  有理由才 view → 失败后简化重试而非乱猜），回归反转为收益：
  **平均评审成本降 ~20%，质量不变**。
- 三条可迁移结论：① **工具表面是产品的一部分**——指令不匹配任务，
  好工具反而帮倒忙；② **基准要看 trace 不只看分**（agent 是收窄还是发散、
  有没有批量搜索、view 有没有理由）；③ 同一套工具指令搬到 CLI（写代码场景）
  没有收益——**指令必须与任务匹配**，不存在通用最优。

### 8.2 GitHub 开源生态扫描（gh API，2026-08-25）

**头部项目**（本领域相关）：

| stars | 项目 | 定位 |
|---|---|---|
| 68.0k | [colbymchenry/codegraph](https://github.com/colbymchenry/codegraph)（我们的上游） | 预建代码知识图谱，活跃（最近 push 2026-08-22） |
| 48.5k | [Aider-AI/aider](https://github.com/Aider-AI/aider) | repo map 始祖 |
| 40.5k | [DeusData/codebase-memory-mcp](https://github.com/DeusData/codebase-memory-mcp) | 单二进制 + Hybrid LSP，158 语言 |
| 30.8k | [tirth8205/code-review-graph](https://github.com/tirth8205/code-review-graph) | **评审专用** tree-sitter 结构图 + 增量 + MCP/CLI |
| 28.5k | [oraios/serena](https://github.com/oraios/serena) | LSP-for-agents 代表 |
| 12.7k | [The-PR-Agent/pr-agent](https://github.com/The-PR-Agent/pr-agent) | Qodo 开源评审 agent 的社区延续 |
| 6.9k | [lintsinghua/DeepAudit](https://github.com/lintsinghua/DeepAudit) | 国产多智能体漏洞挖掘 + 沙箱 PoC 验证 |
| 4.8k | [vitali87/code-graph-rag](https://github.com/vitali87/code-graph-rag) | monorepo 代码图 RAG |
| 3.9k | [sourcebot-dev/sourcebot](https://github.com/sourcebot-dev/sourcebot) | 自托管 Zoekt 代码搜索（人+agent） |
| 3.4k | [joernio/joern](https://github.com/joernio/joern) | CPG 静态分析 |
| 3.3k | [Ataraxy-Labs/sem](https://github.com/Ataraxy-Labs/sem) | 实体级 diff/blame/影响分析（语义版本控制） |
| 1.8k | [sourcegraph/zoekt](https://github.com/sourcegraph/zoekt) | trigram 索引（Snap 同款） |
| 1.5k | [zzet/gortex](https://github.com/zzet/gortex) | 257 语言代码智能引擎 |
| 875 | [github/stack-graphs](https://github.com/stack-graphs) | GitHub 精确导航（最近 push 2025-09，**活跃度降温**） |
| 774 | [google/mantis](https://github.com/google/mantis) | Google 的安全评审 skills 工具包 |
| 574 | [Agent-Field/pr-af](https://github.com/Agent-Field/pr-af) | 自称 Code-Review-Bench 第一的开源评审 agent |

**生态观察**：

1. **赛道拥挤且设计趋同**：大量新项目的共性是"SQLite 持久图 + tree-sitter +
   MCP/CLI 双暴露 + token 节省数字营销"（95%/97%/71× 满天飞）。
   其中 [code-review-graph 附了可复现 benchmark 文档](https://github.com/tirth8205/code-review-graph/blob/main/docs/REPRODUCING.md)
   （143,594 → 2,196 tokens，71×），这是好习惯——别家的数字都该按这个标准自己复现后再信。
2. **code-review-graph 值得专项评估**：定位与我们完全重合（评审场景的上下文
   精简 + GitHub Action + 增量跟踪），但语言覆盖和 C/C++ 深度不如 codegraph
   上游；可作为 codegraph 的对照实现或灵感来源。
3. **DeepAudit** 是漏洞检视方向最活跃的开源参照：多智能体协作审计 +
   **自动化沙箱 PoC 验证**——与 codebadger 的学术路线呼应，"出口验证"思想在
   漏洞域的具体化。
4. **stack-graphs 活跃度降温**（875 stars、近一年无 push）：GitHub 自己的
   精确导航只覆盖少数语言，§5.2（03 篇）建议的 stack graphs 升级路径要
   把这个生态风险计入——它技术优雅但社区投入有限。
5. **评审 agent 的专用基准开始出现**（Code-Review-Bench、
   [withmartian/code-review-benchmark](https://github.com/withmartian/code-review-benchmark)）：
   评估标准化趋势与 DoorDash DashBench 同向，未来选型可直接引用公开基准对比。

### 8.3 对 03/04 篇结论的修正与补强

- **补强**：03 篇"持久化知识图谱是最活跃赛道"被 gh 数据直接证实
  （codegraph 68k、codebase-memory-mcp 40.5k 且都在高速迭代）；
  我们 fork 的上游健康度良好，跟进策略可继续。
- **修正**：03 篇 §5.2 对 stack graphs 的推荐需降半档——技术正确但
  上游投入有限，C/C++ 名称绑定规则大概率要自己写且缺社区支援；
  优先级让位于 Hybrid LSP（codebase-memory-mcp，40.5k stars 活跃）。
- **新增**：Copilot 的教训（§8.1）为我们工具链的 MCP 工具设计立了一条规矩：
  **每个工具的 description 必须按"评审任务"写，不能沿用通用编码助手的措辞**；
  且评估必须看 agent 的调用 trace，不只看最终结论。

---

## 附：来源清单（更新）

- GitHub 官方：[Blackbird 代码搜索史](https://github.blog/engineering/architecture-optimization/a-brief-history-of-code-search-at-github/)、
  [Better tools made Copilot code review worse](https://github.blog/ai-and-ml/github-copilot/better-tools-made-copilot-code-review-worse-heres-how-we-actually-improved-it/)、
  [60 million Copilot code reviews](https://github.blog/ai-and-ml/github-copilot/60-million-copilot-code-reviews-and-counting/)
- GitHub 生态：`gh` CLI/API 直连采集（2026-08-25），仓库链接见 §8.2 表格
- Snap：[CodePal (Part 1)](https://eng.snap.com/codepal)、
  [Code Search (Part 2)](https://eng.snap.com/code_search)
- DoorDash：原文 [评审 agent](https://careersatdoordash.com/blog/doordash-built-an-ai-code-reviewer-engineers-actually-listen-to/)
  / [信任与评估](https://careersatdoordash.com/blog/how-we-learned-to-trust-our-ai-code-reviewer-at-doordash/)（反爬），
  综述 [ZenML 案例一](https://www.zenml.io/llmops-database/building-a-production-ai-code-review-agent-with-high-engineer-acceptance)、
  [ZenML 案例二](https://www.zenml.io/llmops-database/building-trust-in-ai-code-review-through-comprehensive-benchmarking)
- Unblocked：[Context as a first-class system](https://getunblocked.com/blog/building-ai-code-review-with-context-as-a-first-class-system/)
- 国内：[掘金：如何用 AI 做业务级 Code Review](https://juejin.cn/post/7611165150921621554)
- Claude Code 实证：[KaraxAI 拆解](https://karaxai.com/posts/how-claude-code-works-systems-deep-dive/)、
  [vadim.blog](https://vadim.blog/claude-code-no-indexing/)
- 反方：[Milvus: Why I'm Against Claude Code's Grep-Only Retrieval](https://milvus.io/blog/why-im-against-claude-codes-grep-only-retrieval-it-just-burns-too-many-tokens.md)

# 行业调研篇：业内的代码上下文提取与 AI 代码检视怎么做

> 调研时间 2026-08。方法：公开技术博客、官方文档、学术论文（arXiv/ISSTA）、开源仓库源码与对比评测。
> 概念框架见《01-概念篇》，自有工具链见《02-实践篇》；本篇回答"别人怎么做"，
> 最后映射回我们的 codegraph + navmap 决策。
> 所有关键论断附来源链接，可逐一核查。

---

## 0. 一页结论

业内做"给 AI 喂代码上下文"的路线可归纳为 **6 条**，没有银弹，主流正在向
**"分层混合 + 持久化结构索引 + 按需语义"** 收敛：

| 路线 | 代表 | 一句话 |
|---|---|---|
| A. 零索引 agentic 搜索 | Claude Code、Codex | 不建索引，LLM 驱动 ripgrep 逐轮收窄 |
| B. Repo Map | aider | tree-sitter + PageRank + token 预算的"仓库缩略图" |
| C. Embedding RAG | Cursor、Augment | AST 分块 + 向量检索，Merkle 树增量同步 |
| D. LSP-for-agents | Serena、mcp-language-server | 把 IDE 语义能力经 MCP 暴露给 agent |
| E. 持久化代码知识图谱 | GitHub stack graphs、GitNexus、codebase-memory-mcp、CodeGraph | 预建图，agent 查询结构化答案 |
| F. CPG/关系化静态分析 | CodeQL、Joern、codebadger | AST+CFG+PDG 合一，查询语言挖漏洞 |

与我们决策最相关的三个行业信号（详见 §5）：

1. **"不跑 language server 的轻量语义层"（Hybrid LSP）已成为明确趋势**——
   codebase-memory-mcp 用内嵌 C 实现的类型解析替代 language server 进程，
   正是对"clangd 太重"这一痛点的工业级回应，佐证了我们弃用 clangd 路线的方向；
2. **GitHub stack graphs 证明了"无构建精确导航"可行**——不用编译也能做
   名称绑定解析，这是介于 tree-sitter 和 clangd 之间的被低估的中间层；
3. **navmap 的领域分发表显式化在业界几乎没有对应物**——商业产品
   （Greptile/CodeRabbit/Qodo）全部不做这一层，这是我们的差异化资产。

---

## 1. 路线 A：零索引 agentic 搜索（Claude Code / Codex 哲学）

**做法**：不给 agent 任何预建索引，只给文件系统工具（Glob/Grep/Read），
由 LLM 自己驱动 ripgrep 做"宽搜 → 收窄 → 精读"的多轮探索
（[Claude Code Doesn't Index Your Codebase](https://vadim.blog/claude-code-no-indexing/)、
[拆解 Claude Code 的上下文工程](https://juejin.cn/post/7635264373470347299)）。
学术侧对 Claude Code 检索行为的实证研究也确认了这种"三阶段收窄"模式
（[preprints.org 手稿](https://www.preprints.org/manuscript/202510.0924/download/final_file)）。

**学术背书**：浙大团队的 GrepRAG 论文（ISSTA'26）系统评估后得出结论——
在仓库级代码补全任务上，**grep 式词法检索超过 embedding RAG 和结构化图方法，
EM 相对提升 7.04–15.58%，且亚秒级延迟**
（[arXiv:2601.23254](https://arxiv.org/html/2601.23254)、
[ZJU-ACES-ISE/greprag](https://github.com/ZJU-ACES-ISE/greprag/blob/main/README.md)）。
作者的解释：代码标识符本身就是语义，精确匹配恰好最有效。

**局限**：每次会话从零探索，token 成本高、长任务不稳定；社区共识是它
"搜索代码"而非"导航代码"（[Antão Almada 的对比长文](https://antaoalmada.dev/posts/Code-Agent-Knowledge-Graphs/)：
*letting an agent grep your codebase from scratch every session is expensive and error-prone*）。

**对我们的意义**：我们的 agent 原生 Grep 层与 Claude Code 同构，这一层免费获得；
GrepRAG 的结论提示：**不必为"没有 embedding 语义搜索"焦虑**，词法+结构已足够强。

## 2. 路线 B：Repo Map（aider 的仓库缩略图）

**做法**：aider 每次会话前用 tree-sitter 提取全仓定义/引用标签，构建
NetworkX 图（节点=文件，边=跨文件依赖），跑 **PageRank**（以对话中提到的文件
做个性化）排出"此刻最重要的符号"，在 **token 预算**（默认 1024）内输出签名级缩略图
（[mache 竞品分析](https://github.com/agentic-research/mache/blob/main/docs/reference/competitive-landscape.md)、
[goldfish（aider repomap 的 Go 移植）](https://github.com/dereira/goldfish)）。
aider 以此支撑每周 15B token 的生产规模，是被验证最充分的轻量方案
（[hermes-agent issue #535](https://github.com/NousResearch/hermes-agent/issues/535)）。

**关键设计**：不持久化、无 MCP、无查询接口——它就是 context 组装管道的一环。
pydantic、NousResearch 等多个 agent 框架都在复刻这一能力
（[pydantic-ai-harness #73](https://github.com/pydantic/pydantic-ai-harness/issues/73)）。

**对我们的意义**：repo map 的"PageRank 排序 + token 预算裁剪"与我们
CodeAtom IR 的 Priority + CropBudget 是同构思想——**"先排序后裁剪"是行业共识**。
navmap 产物可作为 repo map 的高优先级注入源。

## 3. 路线 C：Embedding RAG（Cursor / Augment）

**Cursor**：本地 tree-sitter AST 分块（不支持的语言回退正则切分）→ 服务端
embedding（自研模型）→ Turbopuffer 向量库；**Merkle 树**做增量同步，
只上传变更文件；约 10 分钟级更新
（[mache 竞品分析](https://github.com/agentic-research/mache/blob/main/docs/reference/competitive-landscape.md)、
[Cursor 索引机制解读](https://tool.lu/en_US/article/7aC/preview)）。
其 embedding 模型用 agent 会话反馈闭环训练（哪些文件最终被需要）。

**Augment Code**：Context Engine 支持 40 万+ 文件、秒级实时索引、
自研代码 embedding（非通用 OpenAI embedding）
（[Augment Context Engine](https://www.augmentcode.com/context-engine)、
[Codacy 的拆解](https://blog.codacy.com/ai-giants-how-augment-code-solved-the-large-codebase-problem)）。

**已知问题**：embedding 在代码编辑后即刻过期（stale-on-edit），
活跃仓库上维护向量索引本身是一条昂贵流水线
（[Graph RAG vs vector RAG for code](https://zzet.org/gortex/graph-rag-vs-vector-rag-for-code/)）；
加上 GrepRAG 的负面证据，纯向量 RAG 路线在代码域的统治力正在被动摇。

**对我们的意义**：我们未引入 embedding 层，目前看不构成短板；
若未来要加"自然语言找代码"，优先选 GitNexus 式 BM25+向量混合而非纯向量。

## 4. 路线 D：LSP-for-agents（Serena / mcp-language-server）

**做法**：把 language server 的符号级能力（定义/引用/符号检索/重命名）经 MCP
暴露给 agent。代表 [oraios/serena](https://github.com/oraios/serena)
（30+ 语言，符号级检索+编辑，我们 mcp-language-server 的上游同族）。
社区的 LSP 集成模式研究总结了一条**能力退化层级**：
*LSP → tree-sitter → grep，LSP 永不可假设可用*（language server 需单独安装、
坐标精度问题在自主 agent 流程中频繁失败）
（[liatrio-labs LSP 集成模式](https://github.com/liatrio-labs/claude-code-gauntlet/blob/main/docs/research/artifacts/06-lsp-integration-patterns.md)）。

**对我们的意义**：这条路线就是我们的旧生产方案，其痛点（索引重、
完整性不可验证）在业界同样被认知——业界对此的回应不是"把 LSP 做得更好"，
而是分化出两个替代品：**Hybrid LSP**（路线 E 的 codebase-memory-mcp）
和 **stack graphs**（§5.2）。

## 5. 路线 E：持久化代码知识图谱（当前最活跃的赛道）

### 5.1 开源四杰对比

来自 [Antão Almada 的系统对比](https://antaoalmada.dev/posts/Code-Agent-Knowledge-Graphs/)
（2026-07，Graphify/GitNexus/codebase-memory-mcp/CodeGraph 四工具横评）：

| | GitNexus | codebase-memory-mcp | CodeGraph（我们的上游） | Graphify |
|---|---|---|---|---|
| 语言 | 14+ | **158** | 20+ | 36 + 文档/图片/视频 |
| 语义层 | 部分 | **Hybrid LSP（10 语言）** | 无 | 无 |
| 特色 | 执行流追踪、**PDG 查询、污点分析**、跨仓契约 | 单静态二进制、Linux 内核 3 分钟索引、DATA_FLOWS 边 | **单工具 explore**、17 框架路由、跨语言桥接 | 多模态语料统一图 |
| 存储 | LadybugDB(Kuzu) | 内嵌 + openCypher | SQLite+FTS5 | JSON/HTML 产物 |
| License | PolyForm NC | MIT | MIT | MIT |

三个值得单独说的设计：

1. **codebase-memory-mcp 的 Hybrid LSP**：用 C 重写的轻量类型解析
   （兼容 tsserver/pyright/gopls 等算法行为），**不起 language server 进程**
   就能把 `user.profile.display_name()` 解析到跨模块声明——
   这正是"clangd 太重"的工业级解法：要语义，但不要编译器前端的全部代价
   （[同上对比文](https://antaoalmada.dev/posts/Code-Agent-Knowledge-Graphs/)）。
   学术评估：31 个真实仓库上 83% 答案质量、**token 降 10×**、工具调用降 2.1×
   （[arXiv:2603.27277](https://arxiv.org/html/2603.27277v1)）。
2. **GitNexus 的 PDG/污点即服务**：`pdg_query`（语句级控制/数据依赖查询）、
   `explain`（source→sink 污点流）作为 MCP 工具暴露——
   **证明 PDG 层可以产品化给 agent 用**，这是我们 §4.3 缺口清单里
   "PDG 层空缺"的一个现成参照实现。
3. **CodeGraph（我们 fork 的上游）的单工具哲学**：只暴露一个 `codegraph_explore`
   ——"一个好工具比一菜单窄工具产生更好的 agent 行为"，基准显示
   工具调用减 58%、提速 22%、文件读取趋零。

### 5.2 GitHub stack graphs：被低估的中间层

GitHub 为其 Precise Code Navigation 开发的
[stack graphs](https://arxiv.org/pdf/2211.01224)（scope graphs 的扩展）：
用声明式 DSL 描述语言的**名称绑定规则**，从 tree-sitter 解析结果生成图，
**不需要构建、不需要 CI、不需要仓库任何配置**，就能做跨文件乃至
**跨仓库**的定义/引用精确解析
（[GitHub Blog: Precise code navigation](https://github.blog/news-insights/product-news/precise-code-navigation-python-code-navigation-pull-requests/)）。

**这是介于 tree-sitter（纯语法）和 clangd（全语义）之间的正式中间层**：
精确到名称绑定，但不碰类型系统，因此无构建依赖、可增量、可规模化到
GitHub 全站。我们弃用 clangd 后如果仍需要"比启发式准"的符号解析，
stack graphs 是学术和工业上最正统的答案——代价是要为 C/C++ 写名称绑定规则
（工作量集中在宏与头文件包含语义）。

### 5.3 Sourcegraph SCIP：编译器索引的"产物化"路线

Sourcegraph 的 [SCIP](https://sourcegraph.com/blog/cross-repository-code-navigation)
与 LSIF 一脉相承：由各语言的**索引器**（通常包装编译器）离线产出索引文件，
上传后提供精确导航。即"clangd 索引"的离线产物化形态——把昂贵的索引
从交互路径挪到 CI。我们的 clangd 逃生通道若将来要服务多人，
SCIP 化（索引产物共享而非每人一份 clangd）是标准解法。

## 6. 路线 F：CPG / 关系化静态分析（安全向）

- **Joern**：开源 CPG 平台，AST+CFG+PDG 合一入图数据库，Scala 查询，
  面向漏洞挖掘（[Joern 项目](https://github.com/DarkerSquirrel/joern-code_property_graphs)）。
  CPG 概念本身（AST+CFG+PDG 联合数据结构，2013 年起源于哥廷根大学的 C/C++ 分析）
  正是我们概念篇 §6 各图的统一封装。
- **codebadger**（arXiv:2603.24837）：把 Joern CPG 包成 **MCP server** 给 LLM
  做 agentic 漏洞分析，已能完成"定位→假设→PoC 验证→补丁"的闭环
  （[arXiv:2603.24837](https://arxiv.org/html/2603.24837v1)）——
  这是"vul-llvm 类深度分析接 MCP"的学界样板。
- **naptrace**：变体分析开源工具——喂一个 CVE 补丁，经
  "补丁摄取→LLM 签名提取→embedding→结构匹配"六级管道找出全仓结构孪生
  （[naptrace](https://github.com/hamzamiladin/naptrace)），
  与漏洞检视的"已知缺陷模式排查"场景直接相关。
- **CodeQL / CodeFuse-Query**：Datalog 流派见概念篇 §7，此处不重复。

## 7. 商业 AI 代码评审产品的上下文工程

| 产品 | 上下文策略 | 来源 |
|---|---|---|
| **CodeRabbit** | 非线性 pipeline：上下文三分（**Intent** PR 意图 / **Environment** 文件关系与依赖 / **Conversation**），从数十个来源拉取 + **验证 agent 二次核对**每条建议 | [CodeRabbit 官方博客](https://www.coderabbit.ai/blog/the-art-and-science-of-context-engineering) |
| **Greptile** | **全仓 code graph**（函数/类/文件/目录连接），先建图再审 PR，拿更高误报换更深上下文；学习团队编码标准 | [Theseus 论文调研](https://www.theseus.fi/bitstream/10024/903761/2/Gronroos_Hietanen_Salmi_Turtianen.pdf)、[augmentcode 对比](https://www.augmentcode.com/tools/coderabbit-vs-greptile-vs-augment-cosmos) |
| **Qodo PR-Agent** | **开源版纯 diff 文本 + LLM，零 AST**（源码级辟谣）；商业版 Qodo Merge 才有多仓上下文/AST 能力 | [CSDN 源码剖析](https://gitcode.csdn.net/6a117faa10ee7a33f274a082.html)、[Qodo 官方对比](https://www.qodo.ai/blog/qodo-vs-coderabbit/) |
| **Cursor Bugbot / Copilot review** | 复用各自 IDE 的索引（embedding/必应代码搜索） | — |

三个跨产品的共识：

1. **上下文三分法**：意图（PR 描述/issue）+ 环境（代码结构）+ 对话——
   我们的工具链只覆盖"环境"，"意图"层（检视目标、需求背景）需要 prompt 工程补；
2. **Goldilocks 问题**：上下文太少幻觉、太多稀释信号——与 CodeAtom IR
   的预算裁剪是同一问题的不同表述；
3. **验证 agent 回路**： CodeRabbit 每条建议都过二次核对 agent——
   这为我们的"启发式调用边静默错误"提供了工程解法：**不是源头做到完美，
   而是出口加一道验证**。

## 8. 学术研究线：SWE-bench 定位脚手架

自动修 bug 研究的"定位"环节就是代码上下文提取的竞技场
（[Clade 研究综述](https://github.com/shenxingy/Clade/blob/main/docs/research/README.md)）：

- **Agentless**：Localize→Repair→Validate 显式分阶段——定位用分层收窄
  （文件→类/函数→行），证明**简单层级结构 + LLM 直觉**即可达 SOTA；
- **AutoCodeRover**：把 AST 结构封装成 **7 个可调用搜索 API** 给 agent
  （按类/方法/签名检索），+ 光谱错误定位（SBFL）预筛——
  与我们"工具面少而精"的设计一致；
- **Moatless Tools**：span 级 FileContext + **token 预算管理** + 语义索引——
  同样是"排序后裁剪"。

## 9. 横断洞察：六个行业共识

1. **分层混合是唯一答案**。纯 grep（Claude Code）和纯图（早期 RAG 派）
   都在向中间靠拢：grep 派加 repo map，图派保留全文检索兜底。
   我们的 codegraph+navmap+原生 Grep 三层与收敛方向一致。
2. **token 预算是第一等约束**。aider 的 1024 token、CodeAtom 的 8KB、
   CodeGraph 的"响应按答案尺寸而非文件数"——所有方案都显式建模预算。
3. **CLI 与 MCP 双暴露**成为标配：紧循环里 CLI 省协议开销，
   结构化交互用 MCP（[Almada 对比文的 CLI 章节](https://antaoalmada.dev/posts/Code-Agent-Knowledge-Graphs/)）。
4. **完整性要可验证**：GitNexus 用 PostToolUse hook 检测索引过期并提示重建；
   codebase-memory-mcp 主打"确定性——同输入同图"。这正是我们弃用
   clangd 的第一理由，业界同频。
5. **Hooks 注入优于工具调用**：GitNexus 的 PreToolUse hook 在 agent 的
   Grep/Glob 结果返回给模型**之前**自动掺入图上下文——
   navmap 产物注入可以借鉴这种"不依赖 agent 自觉查询"的模式。
6. **验证回路弥补检索不完美**：CodeRabbit 的验证 agent、codebadger 的
   PoC 验证，都是"出口验证"而非"源头完美"。

## 10. 映射回我们的决策

| 我们的组件 | 行业对应 | 位置判断 |
|---|---|---|
| agent 原生 Grep | Claude Code / GrepRAG | 主流基线，免费获得 |
| codegraph | 开源图谱四杰之一（我们就在这条船的上游） | 选型正确；C/C++ 硬化上游投入罕见 |
| navmap | **几乎无对应物** | 差异化资产；商业产品全不做领域分发表 |
| mcp-language-server（逃生通道） | Serena / SCIP 化 | 方向正确；多人化时考虑 SCIP 产物共享 |
| vul-llvm | Joern/codebadger（CPG+MCP） | codebadger 证明了该形态可行 |

三个值得跟进的具体事项：

1. **评估 stack graphs for C/C++**（§5.2）：若"启发式调用边"在对照实验中
   误差不可接受，这是不回到 clangd 的前提下最正统的升级路径；
2. **借鉴 codebase-memory-mcp 的 Hybrid LSP 思路**（§5.1）：轻量类型解析
   不起 language server 进程——它是开源的（MIT），可直接研究甚至复用；
3. **navmap 注入改 hook 模式**（§9.5）：把 navmap 产物从"agent 记得去查"
   变成"Grep/Read 结果自动附带"，消除对 agent 自觉性的依赖；
4. **加一道验证回路**（§9.6）：检视结论过二次核对 agent，
   这是业界对"启发式边静默错误"的标准答案，与我们 §6.7 的流程纪律互补。

---

## 附：主要信息源

- 工具横评：[Antão Almada: Knowledge Graph Tools for AI Code Agents](https://antaoalmada.dev/posts/Code-Agent-Knowledge-Graphs/)、
  [mache competitive-landscape](https://github.com/agentic-research/mache/blob/main/docs/reference/competitive-landscape.md)
- 零索引派：[Claude Code no-indexing](https://vadim.blog/claude-code-no-indexing/)、
  [掘金：拆解 Claude Code 上下文工程](https://juejin.cn/post/7635264373470347299)
- 学术：[GrepRAG / Better Call Grep (ISSTA'26)](https://arxiv.org/html/2601.23254)、
  [Stack graphs (GitHub)](https://arxiv.org/pdf/2211.01224)、
  [Codebase-Memory (arXiv:2603.27277)](https://arxiv.org/html/2603.27277v1)、
  [codebadger (arXiv:2603.24837)](https://arxiv.org/html/2603.24837v1)
- 商业产品：[CodeRabbit 上下文工程](https://www.coderabbit.ai/blog/the-art-and-science-of-context-engineering)、
  [Augment Context Engine](https://www.augmentcode.com/context-engine)、
  [PR-Agent 源码剖析（CSDN）](https://gitcode.csdn.net/6a117faa10ee7a33f274a082.html)
- 基础设施：[Sourcegraph SCIP 跨仓导航](https://sourcegraph.com/blog/cross-repository-code-navigation)、
  [LSP 集成模式](https://github.com/liatrio-labs/claude-code-gauntlet/blob/main/docs/research/artifacts/06-lsp-integration-patterns.md)

# 评测篇：AI 代码检视系统的评测方法学

> 调研时间 2026-08。回答一个问题：**怎么科学地评"我们的检视系统好不好"**。
> 覆盖：公开基准全景、基准构建的三种方法学、指标设计、LLM-as-judge 校准、
> 以及映射到 codegraph+navmap 架构的五层评测方案。
> 与《05 落地建议》配套：05 的每个 Phase 出口标准，评测方法都出自本篇。

---

## 0. 一页结论

1. **没有现成基准能直接评我们的系统**——公开基准面向通用语言/通用场景，
   我们是宏控 C/C++ + 表驱动分发，**自建评测集不可避免**；公开基准的价值在
   方法学和工具对照，不在分数。
2. **基准构建有且只有三种方法学**：fix-commit 回溯、缺陷注入、真实事故回放。
   三者各有系统性偏差，**要混用**（§2）。
3. **指标的第一坑是 Hit 的定义**：必须"文本描述对 + 定位对"双条件才算命中，
   否则分数虚高（§3.1）。第二坑是只看采纳率——它只覆盖混淆矩阵的两格（§3.3）。
4. **precision 可以后处理调，recall 才是系统能力的硬约束**（Qodo 的核心论点，
   §3.2）——这与我们"宽进严出"的漏斗架构互为因果。
5. **评估必须分层**：组件级（调用边准不准）与系统级（评审结论好不好）是
   两种评测，混为一谈会导致归因失败（§5）。

---

## 1. 公开基准全景

### 1.1 评审 agent 基准（与本场景最相关）

| 基准 | 来源 | 方法学 | 规模 | 特点 |
|---|---|---|---|---|
| [CR-Bench](https://arxiv.org/abs/2603.11078) | Nutanix | 真实 PR + 细粒度评估管线（CR-Evaluator） | — | 显式以"false positive 代价高"为设计前提 |
| [c-CRAB](https://arxiv.org/abs/2603.23448) | 学术界 | **人类评审反馈转成可执行测试** | — | 客观验证评审质量；当前 agent 只解 ~40% |
| [CodeFuse-CR-Bench](https://arxiv.org/abs/2509.14856) | 蚂蚁 CodeFuse | 仓库级端到端、上下文丰富 | 601 例（Python） | 首个"完整性感知"基准；与 CodeFuse-Query 同族 |
| [Martian Code Review Bench](https://github.com/withmartian/code-review-benchmark) | Martian | 真实世界数据 + 防记忆设计 | 号称最大开源 | Qodo F1 64.3% 居首；[CodeRabbit 自称第一](https://www.coderabbit.ai/blog/coderabbit-tops-martian-code-review-benchmark)——厂商打架中，分数仅供参考 |
| [Qodo Benchmark 1.0](https://www.qodo.ai/blog/how-we-built-a-real-world-benchmark-for-ai-code-review/) | Qodo | **缺陷注入真实已合并 PR** | 100 PR / 580 issue | 双焦点（正确性+规范）；方法学描述最完整 |

### 1.2 定位/探索能力基准（评"上下文提取"这一层）

- [SWE-ContextBench](https://arxiv.org/html/2602.08316v3)：显式评 coding agent 的
  **上下文理解与检索**能力（而非最终修复率）；
- [SWE-Explore](https://arxiv.org/html/2606.07297v1)（上海交大等）：把代码搜索
  从修复任务中解耦，首次量化 agent 的**行级定位缺陷**——
  "到没到对的文件/函数"和"到没到对的行"是两件事；
- 惯例指标：SWE-bench 系数据集上的 file-level / function-level / line-level
  localization accuracy。

### 1.3 静态分析/漏洞检测测试集（评"定罪"层）

- [NIST Juliet / SARD](https://samate.nist.gov/SARD/)：合成用例集，
  显式标注 FP/FN，是 SAST 工具评估的事实标准；
  [NIST SATE 十年报告（SP 500-326）](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.500-326.pdf)
  同时记录了它的局限——合成代码与真实缺陷分布差异大；
- [OWASP Benchmark](https://owasp.org/www-project-benchmark/)：TPR/FPR 标注集。
  参考数据点：NIST 测得 SAST 假阳性率 Java 高达 78%。
- **用法定位**：适合做**回归门禁**（改动后能力不退化），
  不适合做绝对能力宣称（合成≠真实）。

---

## 2. 基准构建的三种方法学（核心知识）

### 2.1 Fix-commit 回溯（Greptile/Augment 采用）

做法：找到修复 bug 的 commit，回到修复前版本，看工具能否发现这个
"历史上真实存在过的 bug"。
**优点**：缺陷真实。**缺点**（Qodo 的批评）：每例通常只有一个 bug、
丢失完整 PR 的规模与上下文、偏向"能被一个 commit 修掉"的简单缺陷、
规模做不大。

### 2.2 缺陷注入（Qodo Benchmark 1.0，目前方法学描述最完整）

做法（[官方博客](https://www.qodo.ai/blog/how-we-built-a-real-world-benchmark-for-ai-code-review/)）：

1. 选生产级多组件开源仓（TS/Python/JS/C/C#/Rust/Swift）；
2. **先从仓里提取该仓专属的 best-practice 规则**（文档+全仓分析+人工校验）；
3. 严选"干净"PR：近期合并、3+ 文件、50-15000 行、**无后续 revert 或
   补救 commit**；
4. LLM 注入两类缺陷：规范违反（破坏 diff 但保持功能）+ 1~3 个功能 bug
   （逻辑错误/边界/竞态/资源泄漏/错误处理）；
5. **双重人工校验**，校验中发现的"自然存在"的额外问题也补进 ground truth。

**优点**：可控、可规模化、仓无关、可同时评正确性与规范性。
**缺点**：注入的缺陷分布≠真实缺陷分布；LLM 注入的 bug 有"LLM 味儿"
（对 LLM 评审可能偏简单或偏模式化）。

### 2.3 真实事故/漏报回放（DoorDash DashBench，04 篇 §2.4）

做法：从生产漏报、线上事故、被 revert/hotfix 的 PR 回溯建集，
**外加良性 PR 测克制力**；标注用原作者+LLM judge+人工仲裁三方三角验证。
**优点**：分布最真实，直接对齐业务损失。**缺点**：建集慢、需要生产积累、
标注仲裁成本高。

### 2.4 我们的选择：混合

- **主干用 2.3**（真实事故/漏报回放 + 良性样本）——与业务损失直接对齐，
  05 篇 Phase 0 的 20~30 例就是这个；
- **规模扩充用 2.2**（在我们自己的仓上做缺陷注入）——
  Qodo 证明了该流程仓无关、可复制，且能定向注入**我们关心的缺陷类型**
  （宏分支错误、分发链路破坏、状态机迁移遗漏——这些是通用基准里
  根本不存在但我们天天踩的类别）；
- **2.1 顺手做**：从 git 历史里筛 fix-commit 自动扩充候选，人工确认后入集。

---

## 3. 指标设计

### 3.1 Hit 的定义是第一坑

Qodo 的定义值得直接采用——一条评论算命中（TP）当且仅当：

1. **文本准确**：评论正确描述了底层问题；**且**
2. **定位正确**：file + line 指向问题源头。

只满足其一是假阳性。很多工具的高分来自放松第二条
（问题说对了但指错了行）。SWE-Explore 的发现（§1.2）说明
行级定位恰恰是当前 agent 的短板，不卡这条等于放水。

### 3.2 precision 可调，recall 是硬约束

Qodo 的核心论点：precision 可以靠后处理（更严的过滤）按用户偏好调，
**recall 受限于系统对代码库、跨文件依赖、架构上下文的理解深度，
后天补不回来**。这给我们的漏斗架构（宽进→严出）一个评测推论：

> **分层计指标**：漏斗每一级单独算 recall（这一级有没有把真问题漏掉），
> 只在最终出口算 precision。否则"召回差"会被"过滤强"掩盖，
> 你分不清是检索层漏了还是验证层砍了。

### 3.3 生产指标三角（DashBench，04 篇 §2.4）

- **采纳率**只覆盖混淆矩阵两格（TP/FP），漏报（FN）和正确的沉默（TN）不可见；
- **采纳 ≠ 真实**：作者会因赶工、改法侵入性等拒绝真实发现；
- 必须三路信号并采：被采纳（TP）、被👎（FP）、**被忽略**（可能是真发现但
  没被重视——Snap 单独统计）、**被修复**（最强 TP 信号）；
- 按严重度加权（DashBench：critical=4, high=2, medium=1, low=0.5），
  漏一个 critical 和漏一个 low 不能等值。

### 3.4 成本与时延

- **cost per successful review**，不是 token 单价
  （弱模型输出非法 JSON 反复重试更贵，04 篇 §2.4）；
- 时延对标"人类首次评审时间"而不是绝对值（Snap：10min vs 人类 5h）。

---

## 4. LLM-as-judge：可以用，必须校准

各方共识（DashBench + Qodo + c-CRAB）：

- 纯人工标注不可扩展（原作者标自己的 PR 都会错——DashBench 实测
  "人类反馈经常错"）；纯 LLM judge 有模型偏见。
- 可行姿势：**judge 当"校准过的信号"而非 ground truth**——
  固定 rubric、保留人工审计子集、人机分歧处人工仲裁、
  仲裁结果回灌 judge 校准（DashBench 的 disagreement review 循环）。
- c-CRAB 提供了更硬的替代：**把人类评审意见转成可执行测试**，
  能跑测试验证的就不依赖 judge——在我们的 C/C++ 场景，
  编译级检查（缺陷注入后是否编译行为变化）也可作为部分 ground truth 的
  自动化来源。

---

## 5. 映射到我们的架构：五层评测方案

| 层 | 评测对象 | 方法 | 指标 |
|---|---|---|---|
| L1/L2 检索层 | codegraph 符号/引用/调用边 | Phase 0 四方对照（codegraph vs agentic 实时 vs clangd 真值 vs navmap），抽样人工裁定 | 调用边 precision/recall、定位准确率（行级） |
| L2.5 领域层 | navmap 表项 | 与源码逐一核对（已有传统：u-boot 实测"抽查表项与源码逐一吻合"）；虚拟边注入后重跑 L2 对照 | 表项准确率、handler USR 解析率、分发链路连通率 |
| Intent 层 | 事故复盘 RAG | RAGAS 式 context precision/recall：注入的历史案例是否真的相关 | 注入率、人工判相关率、阈值 ≥0.85 的边际分析 |
| 评审回路 | 端到端结论 | 混合基准（§2.4）：真实回放 + 自仓注入 + 良性样本；LLM judge + 人工仲裁 | 分层 recall、出口加权 precision/F1、良性零误报 |
| 生产层 | 上线后真实价值 | 反馈飞轮（👍/👎/忽略/修复四信号） | 加权采纳率、忽略率、修复率、cost/successful review、时延 |

三个实施要点：

1. **组件级评测先行**（Phase 0）：调用边信不过，端到端分数无法归因。
   这是 05 篇把对照实验放在第一步的原因，本篇给了它指标形态。
2. **回归门禁用合成集，能力宣称用真实集**：Juliet/自仓注入集进 CI
   （每次改动不退化），真实事故集做季度能力评估（对外汇报用后者）。
3. **trace 级审计进评估流程**（GitHub 教训，04 篇 §8.1）：
   每例不仅记结论对错，还记 agent 的调用轨迹——是收窄还是发散、
   有没有批量搜索、读文件有没有理由。分数异常时靠 trace 归因。

---

## 6. 陷阱清单

1. **厂商基准打架**：Qodo 和 CodeRabbit 在同一个 Martian 基准上
   都自称第一——公开基准分数只能看趋势，不能当选型依据；
   且基准作者与厂商利益相关时（Qodo 自建基准 Qodo 第一）更要打折。
2. **数据泄漏/记忆**：模型可能在训练数据里见过评测用的开源仓 PR
   （Martian 把"防记忆"作为卖点正说明这问题普遍）。
   **我们用自己的私有仓建集天然免疫**——这是自建集的隐藏优势。
3. **合成≠真实**：Juliet 类合成集上 90% 不代表生产 90%
   （SATE 报告明确其局限）；注入的 bug 有"LLM 味儿"。
4. **采纳率幻觉**：高采纳率可能只是"只报最保守的发现"；
   必须配合良性样本和召回侧指标看。
5. **单指标独裁**：weighted precision/recall/F1、critical 召回、
   成本、时延各自独立变动（DashBench：没有配置全面占优），
   "更好"必须指明在哪个轴上。
6. **judge 漂移**：模型升级后旧 judge 的校准失效，
   仲裁子集要定期重抽（DashBench 的持续校准循环）。

---

## 附：主要来源

- 评审基准：[CR-Bench](https://arxiv.org/abs/2603.11078)、
  [c-CRAB](https://arxiv.org/abs/2603.23448)、
  [CodeFuse-CR-Bench](https://arxiv.org/abs/2509.14856)、
  [Martian Code Review Bench](https://github.com/withmartian/code-review-benchmark)、
  [Qodo Benchmark 1.0 方法学](https://www.qodo.ai/blog/how-we-built-a-real-world-benchmark-for-ai-code-review/)
- 定位评测：[SWE-ContextBench](https://arxiv.org/html/2602.08316v3)、
  [SWE-Explore](https://arxiv.org/html/2606.07297v1)
- 静态分析测试集：[NIST SATE 十年报告](https://nvlpubs.nist.gov/nistpubs/SpecialPublications/NIST.SP.500-326.pdf)、
  [OWASP Benchmark](https://owasp.org/www-project-benchmark/)
- 生产评估实践：DoorDash DashBench、Snap golden dataset（见 04 篇 §2.4/§1.4）
